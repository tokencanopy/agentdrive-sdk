# coding: utf-8

"""
    AgentDrive

    AgentDrive is an agent-focused artifact store: drive-scoped folders, artifacts, and immutable versions, with local grants, possession-based share links, drive-scoped search, and a cursor-resumable change feed. Bearer-authenticated with Hub-issued product tokens (see /.well-known/oauth-protected-resource); every mutation takes an Idempotency-Key, and existing-state mutations take If-Match.

    The version of the OpenAPI document: <PINNED>
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from agentdrive_sdk.models.upload_target_artifact_out import UploadTargetArtifactOut
from agentdrive_sdk.models.upload_target_version_out import UploadTargetVersionOut
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

TARGET_ONE_OF_SCHEMAS = ["UploadTargetArtifactOut", "UploadTargetVersionOut"]

class Target(BaseModel):
    """
    Target
    """
    # data type: UploadTargetArtifactOut
    oneof_schema_1_validator: Optional[UploadTargetArtifactOut] = None
    # data type: UploadTargetVersionOut
    oneof_schema_2_validator: Optional[UploadTargetVersionOut] = None
    actual_instance: Optional[Union[UploadTargetArtifactOut, UploadTargetVersionOut]] = None
    one_of_schemas: Set[str] = { "UploadTargetArtifactOut", "UploadTargetVersionOut" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Target.model_construct()
        error_messages = []
        match = 0
        # validate data type: UploadTargetArtifactOut
        if not isinstance(v, UploadTargetArtifactOut):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UploadTargetArtifactOut`")
        else:
            match += 1
        # validate data type: UploadTargetVersionOut
        if not isinstance(v, UploadTargetVersionOut):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UploadTargetVersionOut`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Target with oneOf schemas: UploadTargetArtifactOut, UploadTargetVersionOut. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Target with oneOf schemas: UploadTargetArtifactOut, UploadTargetVersionOut. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into UploadTargetArtifactOut
        try:
            instance.actual_instance = UploadTargetArtifactOut.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UploadTargetVersionOut
        try:
            instance.actual_instance = UploadTargetVersionOut.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Target with oneOf schemas: UploadTargetArtifactOut, UploadTargetVersionOut. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Target with oneOf schemas: UploadTargetArtifactOut, UploadTargetVersionOut. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], UploadTargetArtifactOut, UploadTargetVersionOut]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
