# coding: utf-8

"""
    AgentDrive

    AgentDrive is an agent-focused artifact store: drive-scoped folders, artifacts, and immutable versions, with local grants, possession-based share links, drive-scoped search, and a cursor-resumable change feed. Bearer-authenticated with Hub-issued product tokens (see /.well-known/oauth-protected-resource); every mutation takes an Idempotency-Key, and existing-state mutations take If-Match.

    The version of the OpenAPI document: 0.0.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ViewerSessionCreateOut(BaseModel):
    """
    The mint response — the ONLY response carrying the plaintext viewer credential. The credential authorizes the isolated viewer host's `/view/doc` and `/view/content` for this one pinned version, via an Authorization header only — it must never be placed in a URL, cookie, or persistent storage.
    """ # noqa: E501
    id: Annotated[str, Field(strict=True)]
    drive_id: Annotated[str, Field(strict=True)]
    artifact_id: Annotated[str, Field(strict=True)]
    version_id: Annotated[str, Field(strict=True)]
    expires_at: datetime
    created_at: datetime
    expires_in: StrictInt = Field(description="Seconds until the session expires, from mint time.")
    credential: Optional[StrictStr] = Field(default=None, description="Plaintext viewer credential. Present only on first execution of a mint; null on idempotent replay — mint a new session with a fresh Idempotency-Key to obtain a credential.")
    __properties: ClassVar[List[str]] = ["id", "drive_id", "artifact_id", "version_id", "expires_at", "created_at", "expires_in", "credential"]

    @field_validator('id', mode="before")
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^vwr_[a-f0-9]{16}$", value):
            raise ValueError(r"must validate the regular expression /^vwr_[a-f0-9]{16}$/")
        return value

    @field_validator('drive_id', mode="before")
    def drive_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^drv_[a-f0-9]{16}$", value):
            raise ValueError(r"must validate the regular expression /^drv_[a-f0-9]{16}$/")
        return value

    @field_validator('artifact_id', mode="before")
    def artifact_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^art_[a-f0-9]{16}$", value):
            raise ValueError(r"must validate the regular expression /^art_[a-f0-9]{16}$/")
        return value

    @field_validator('version_id', mode="before")
    def version_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^ver_[a-f0-9]{16}$", value):
            raise ValueError(r"must validate the regular expression /^ver_[a-f0-9]{16}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ViewerSessionCreateOut from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if credential (nullable) is None
        # and model_fields_set contains the field
        if self.credential is None and "credential" in self.model_fields_set:
            _dict['credential'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ViewerSessionCreateOut from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "drive_id": obj.get("drive_id"),
            "artifact_id": obj.get("artifact_id"),
            "version_id": obj.get("version_id"),
            "expires_at": obj.get("expires_at"),
            "created_at": obj.get("created_at"),
            "expires_in": obj.get("expires_in"),
            "credential": obj.get("credential")
        })
        return _obj


