/* tslint:disable */
/* eslint-disable */
/**
 * AgentDrive
 * AgentDrive is an agent-focused artifact store: upload by path, share by rendered URL, address by stable permalink. The REST surface is documented here; the rendered viewer + agent claim flow live under `agentdrive.run`.
 *
 * The version of the OpenAPI document: <PINNED>
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ArtifactSource } from './ArtifactSource';
import {
    ArtifactSourceFromJSON,
    ArtifactSourceFromJSONTyped,
    ArtifactSourceToJSON,
    ArtifactSourceToJSONTyped,
} from './ArtifactSource';

/**
 * PATCH /v0/artifacts/{art_id} body — metadata-only partial
 * (JSON-merge-patch) update.
 *
 * Every field is optional. Presence is what matters, not the value:
 * a field left out of the body (per Pydantic `model_fields_set`) is
 * left unchanged; a field that IS present is applied — with an explicit
 * `null` / `[]` / `{}` meaning "clear it". This mirrors the MCP
 * `set_metadata` tool and the core `patch_artifact_metadata` sentinel
 * semantics (omitted = preserve, present = replace/clear).
 *
 *   * `labels`   — replace the label set; `[]` or `null` clears it.
 *   * `metadata` — replace the free-form metadata object; `{}` or `null`
 *                  clears it.
 *   * `source`   — replace provenance refs; `null` (or `{"refs": []}`)
 *                  clears them.
 *
 * PATCH is metadata-only: to move/rename an artifact, use
 * `POST /v0/artifacts/{art_id}/move`. `extra="forbid"` makes a stray
 * field (notably a legacy `path`) a hard 422 rather than a silent
 * no-op — a clean-break signal to migrate to the move verb.
 * @export
 * @interface ArtifactPatchIn
 */
export interface ArtifactPatchIn {
    /**
     *
     * @type {Array<string>}
     * @memberof ArtifactPatchIn
     */
    labels?: Array<string> | null;
    /**
     *
     * @type {{ [key: string]: any; }}
     * @memberof ArtifactPatchIn
     */
    metadata?: { [key: string]: any; } | null;
    /**
     *
     * @type {ArtifactSource}
     * @memberof ArtifactPatchIn
     */
    source?: ArtifactSource | null;
}

/**
 * Check if a given object implements the ArtifactPatchIn interface.
 */
export function instanceOfArtifactPatchIn(value: object): value is ArtifactPatchIn {
    return true;
}

export function ArtifactPatchInFromJSON(json: any): ArtifactPatchIn {
    return ArtifactPatchInFromJSONTyped(json, false);
}

export function ArtifactPatchInFromJSONTyped(json: any, ignoreDiscriminator: boolean): ArtifactPatchIn {
    if (json == null) {
        return json;
    }
    return {

        'labels': json['labels'] === undefined ? undefined : json['labels'] === null ? null : json['labels'],
        'metadata': json['metadata'] === undefined ? undefined : json['metadata'] === null ? null : json['metadata'],
        'source': json['source'] === undefined ? undefined : json['source'] === null ? null : ArtifactSourceFromJSON(json['source']),
    };
}

export function ArtifactPatchInToJSON(json: any): ArtifactPatchIn {
    return ArtifactPatchInToJSONTyped(json, false);
}

export function ArtifactPatchInToJSONTyped(value?: ArtifactPatchIn | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {

        'labels': value['labels'],
        'metadata': value['metadata'],
        'source': ArtifactSourceToJSON(value['source']),
    };
}
