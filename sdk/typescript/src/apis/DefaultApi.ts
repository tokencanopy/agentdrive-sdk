/* tslint:disable */
/* eslint-disable */
/**
 * AgentDrive
 * AgentDrive is an agent-focused artifact store: upload by path, share by rendered URL, address by stable permalink. The REST surface is documented here; the rendered viewer + agent claim flow live under `agentdrive.run`.
 *
 * The version of the OpenAPI document: <PINNED>
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ArtifactDeleteOut,
    ArtifactDeleteOutFromJSON,
    ArtifactDeleteOutToJSON,
} from '../models/ArtifactDeleteOut';
import {
    type ArtifactHeadOut,
    ArtifactHeadOutFromJSON,
    ArtifactHeadOutToJSON,
} from '../models/ArtifactHeadOut';
import {
    type ArtifactMoveIn,
    ArtifactMoveInFromJSON,
    ArtifactMoveInToJSON,
} from '../models/ArtifactMoveIn';
import {
    type ArtifactOut,
    ArtifactOutFromJSON,
    ArtifactOutToJSON,
} from '../models/ArtifactOut';
import {
    type ArtifactPatchIn,
    ArtifactPatchInFromJSON,
    ArtifactPatchInToJSON,
} from '../models/ArtifactPatchIn';
import {
    type CompileJobIn,
    CompileJobInFromJSON,
    CompileJobInToJSON,
} from '../models/CompileJobIn';
import {
    type CompileJobListOut,
    CompileJobListOutFromJSON,
    CompileJobListOutToJSON,
} from '../models/CompileJobListOut';
import {
    type CompileJobOut,
    CompileJobOutFromJSON,
    CompileJobOutToJSON,
} from '../models/CompileJobOut';
import {
    type CompileProjectOut,
    CompileProjectOutFromJSON,
    CompileProjectOutToJSON,
} from '../models/CompileProjectOut';
import {
    type CopyIn,
    CopyInFromJSON,
    CopyInToJSON,
} from '../models/CopyIn';
import {
    type DatasetDescriptionOut,
    DatasetDescriptionOutFromJSON,
    DatasetDescriptionOutToJSON,
} from '../models/DatasetDescriptionOut';
import {
    type DescribeIn,
    DescribeInFromJSON,
    DescribeInToJSON,
} from '../models/DescribeIn';
import {
    type DownloadUrlOut,
    DownloadUrlOutFromJSON,
    DownloadUrlOutToJSON,
} from '../models/DownloadUrlOut';
import {
    type DriveDeleteOut,
    DriveDeleteOutFromJSON,
    DriveDeleteOutToJSON,
} from '../models/DriveDeleteOut';
import {
    type DriveReadOut,
    DriveReadOutFromJSON,
    DriveReadOutToJSON,
} from '../models/DriveReadOut';
import {
    type DriveRestoreOut,
    DriveRestoreOutFromJSON,
    DriveRestoreOutToJSON,
} from '../models/DriveRestoreOut';
import {
    type DriveUsageOut,
    DriveUsageOutFromJSON,
    DriveUsageOutToJSON,
} from '../models/DriveUsageOut';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type EventPage,
    EventPageFromJSON,
    EventPageToJSON,
} from '../models/EventPage';
import {
    type FeedbackCreateOut,
    FeedbackCreateOutFromJSON,
    FeedbackCreateOutToJSON,
} from '../models/FeedbackCreateOut';
import {
    type FeedbackStatusOut,
    FeedbackStatusOutFromJSON,
    FeedbackStatusOutToJSON,
} from '../models/FeedbackStatusOut';
import {
    type FindPage,
    FindPageFromJSON,
    FindPageToJSON,
} from '../models/FindPage';
import {
    type FolderCopyIn,
    FolderCopyInFromJSON,
    FolderCopyInToJSON,
} from '../models/FolderCopyIn';
import {
    type FolderCopyOut,
    FolderCopyOutFromJSON,
    FolderCopyOutToJSON,
} from '../models/FolderCopyOut';
import {
    type FolderCreateIn,
    FolderCreateInFromJSON,
    FolderCreateInToJSON,
} from '../models/FolderCreateIn';
import {
    type FolderDeleteOut,
    FolderDeleteOutFromJSON,
    FolderDeleteOutToJSON,
} from '../models/FolderDeleteOut';
import {
    type FolderMoveIn,
    FolderMoveInFromJSON,
    FolderMoveInToJSON,
} from '../models/FolderMoveIn';
import {
    type FolderOut,
    FolderOutFromJSON,
    FolderOutToJSON,
} from '../models/FolderOut';
import {
    type FolderPatchIn,
    FolderPatchInFromJSON,
    FolderPatchInToJSON,
} from '../models/FolderPatchIn';
import {
    type FolderRestoreOut,
    FolderRestoreOutFromJSON,
    FolderRestoreOutToJSON,
} from '../models/FolderRestoreOut';
import {
    type GrantCreateIn,
    GrantCreateInFromJSON,
    GrantCreateInToJSON,
} from '../models/GrantCreateIn';
import {
    type GrantList,
    GrantListFromJSON,
    GrantListToJSON,
} from '../models/GrantList';
import {
    type GrantOut,
    GrantOutFromJSON,
    GrantOutToJSON,
} from '../models/GrantOut';
import {
    type GrantPatchIn,
    GrantPatchInFromJSON,
    GrantPatchInToJSON,
} from '../models/GrantPatchIn';
import {
    type HealthDegradedResponse,
    HealthDegradedResponseFromJSON,
    HealthDegradedResponseToJSON,
} from '../models/HealthDegradedResponse';
import {
    type HealthOut,
    HealthOutFromJSON,
    HealthOutToJSON,
} from '../models/HealthOut';
import {
    type LookupValuesIn,
    LookupValuesInFromJSON,
    LookupValuesInToJSON,
} from '../models/LookupValuesIn';
import {
    type LookupValuesOut,
    LookupValuesOutFromJSON,
    LookupValuesOutToJSON,
} from '../models/LookupValuesOut';
import {
    type Page,
    PageFromJSON,
    PageToJSON,
} from '../models/Page';
import {
    type ProjectConfigIn,
    ProjectConfigInFromJSON,
    ProjectConfigInToJSON,
} from '../models/ProjectConfigIn';
import {
    type QueryIn,
    QueryInFromJSON,
    QueryInToJSON,
} from '../models/QueryIn';
import {
    type ResponsePostQueryV0QueryPost,
    ResponsePostQueryV0QueryPostFromJSON,
    ResponsePostQueryV0QueryPostToJSON,
} from '../models/ResponsePostQueryV0QueryPost';
import {
    type RevokeOut,
    RevokeOutFromJSON,
    RevokeOutToJSON,
} from '../models/RevokeOut';
import {
    type SearchPage,
    SearchPageFromJSON,
    SearchPageToJSON,
} from '../models/SearchPage';
import {
    type ShareCreateIn,
    ShareCreateInFromJSON,
    ShareCreateInToJSON,
} from '../models/ShareCreateIn';
import {
    type ShareErrorOut,
    ShareErrorOutFromJSON,
    ShareErrorOutToJSON,
} from '../models/ShareErrorOut';
import {
    type ShareList,
    ShareListFromJSON,
    ShareListToJSON,
} from '../models/ShareList';
import {
    type ShareMintOut,
    ShareMintOutFromJSON,
    ShareMintOutToJSON,
} from '../models/ShareMintOut';
import {
    type ShareOut,
    ShareOutFromJSON,
    ShareOutToJSON,
} from '../models/ShareOut';
import {
    type ShareRedeemOut,
    ShareRedeemOutFromJSON,
    ShareRedeemOutToJSON,
} from '../models/ShareRedeemOut';
import {
    type TrashOut,
    TrashOutFromJSON,
    TrashOutToJSON,
} from '../models/TrashOut';
import {
    type UploadAbortOut,
    UploadAbortOutFromJSON,
    UploadAbortOutToJSON,
} from '../models/UploadAbortOut';
import {
    type UploadBeginIn,
    UploadBeginInFromJSON,
    UploadBeginInToJSON,
} from '../models/UploadBeginIn';
import {
    type UploadBeginOut,
    UploadBeginOutFromJSON,
    UploadBeginOutToJSON,
} from '../models/UploadBeginOut';
import {
    type UploadStatusOut,
    UploadStatusOutFromJSON,
    UploadStatusOutToJSON,
} from '../models/UploadStatusOut';
import {
    type ValidationErrorResponse,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/ValidationErrorResponse';
import {
    type VersionOut,
    VersionOutFromJSON,
    VersionOutToJSON,
} from '../models/VersionOut';
import {
    type VersionPage,
    VersionPageFromJSON,
    VersionPageToJSON,
} from '../models/VersionPage';

export interface AbortUploadV0UploadsUploadIdDeleteRequest {
    uploadId: string;
}

export interface BeginUploadV0UploadsPostRequest {
    uploadBeginIn: UploadBeginIn;
}

export interface CallbackAuthCallbackGetRequest {
    code?: string | null;
    state?: string | null;
    error?: string | null;
}

export interface CancelJobV0JobsJobIdCancelPostRequest {
    jobId: string;
}

export interface CommitUploadV0UploadsUploadIdCommitPostRequest {
    uploadId: string;
}

export interface CopyArtifactRouteV0ArtifactsArtIdCopyPostRequest {
    artId: string;
    copyIn: CopyIn;
    xAgentdriveActor?: string | null;
    ifNoneMatch?: string | null;
}

export interface CopyFolderByIdV0FoldersFldIdCopyPostRequest {
    fldId: string;
    folderCopyIn: FolderCopyIn;
    xAgentdriveActor?: string | null;
    ifNoneMatch?: string | null;
}

export interface CreateFolderByPathV0FoldersPathPutRequest {
    path: string;
    xAgentdriveActor?: string | null;
    ifNoneMatch?: string | null;
    folderCreateIn?: FolderCreateIn | null;
}

export interface CreateGrantRouteV0GrantsPostRequest {
    grantCreateIn: GrantCreateIn;
    xAgentdriveActor?: string | null;
}

export interface CreateShareRouteV0SharesPostRequest {
    shareCreateIn: ShareCreateIn;
    xAgentdriveActor?: string | null;
}

export interface DeleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequest {
    artId: string;
    ifMatch?: string | null;
    xAgentdriveActor?: string | null;
}

export interface DeleteArtifactV0ArtifactsPathDeleteRequest {
    path: string;
    ifMatch?: string | null;
    xAgentdriveActor?: string | null;
}

export interface DeleteDriveRouteV0DrivesDriveIdDeleteRequest {
    driveId: string;
    confirm?: string | null;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface DeleteFolderByIdV0FoldersFldIdDeleteRequest {
    fldId: string;
    recursive?: boolean;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface DeleteFolderByPathV0FoldersPathDeleteRequest {
    path: string;
    recursive?: boolean;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface DeleteGrantRouteV0GrantsGrnIdDeleteRequest {
    grnId: string;
    xAgentdriveActor?: string | null;
}

export interface DeleteShareRouteV0SharesShrIdDeleteRequest {
    shrId: string;
    xAgentdriveActor?: string | null;
}

export interface DownloadArtifactByIdV0ArtifactsArtIdDownloadGetRequest {
    artId: string;
}

export interface DownloadArtifactByPathV0ArtifactsPathDownloadGetRequest {
    path: string;
}

export interface DownloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequest {
    artId: string;
    versionNumber: number;
}

export interface DownloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequest {
    artId: string;
}

export interface DownloadUrlByPathV0ArtifactsPathDownloadUrlGetRequest {
    path: string;
}

export interface DownloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequest {
    artId: string;
    versionNumber: number;
}

export interface EnqueueJobV0ProjectsFldIdJobsPostRequest {
    fldId: string;
    compileJobIn: CompileJobIn;
    xAgentdriveActor?: string | null;
}

export interface ExtensionStartAuthExtensionStartGetRequest {
    extId?: string | null;
}

export interface FindV0FindGetRequest {
    q: string;
    mode?: FindV0FindGetModeEnum;
    label?: Array<string>;
    fileType?: string | null;
    prefix?: string | null;
    modality?: Array<string>;
    updatedAfter?: Date | null;
    updatedBefore?: Date | null;
    limit?: number;
}

export interface GetArtifactByIdMetaV0ArtifactsArtIdMetaGetRequest {
    artId: string;
}

export interface GetArtifactByIdV0ArtifactsArtIdGetRequest {
    artId: string;
}

export interface GetArtifactMetaV0ArtifactsPathMetaGetRequest {
    path: string;
}

export interface GetArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequest {
    artId: string;
    versionNumber: number;
}

export interface GetDriveRouteV0DrivesDriveIdGetRequest {
    driveId: string;
}

export interface GetFeedbackStatusV0FeedbackFbkIdGetRequest {
    fbkId: string;
}

export interface GetFolderByIdMetaV0FoldersFldIdMetaGetRequest {
    fldId: string;
}

export interface GetFolderByIdV0FoldersFldIdGetRequest {
    fldId: string;
}

export interface GetFolderByPathMetaV0FoldersPathMetaGetRequest {
    path: string;
}

export interface GetFolderByPathV0FoldersPathGetRequest {
    path: string;
}

export interface GetGrantRouteV0GrantsGrnIdGetRequest {
    grnId: string;
}

export interface GetJobLogsV0JobsJobIdLogsGetRequest {
    jobId: string;
}

export interface GetJobV0JobsJobIdGetRequest {
    jobId: string;
}

export interface GetProjectV0ProjectsFldIdGetRequest {
    fldId: string;
}

export interface GetShareRouteV0SharesShrIdGetRequest {
    shrId: string;
}

export interface GetUploadStatusV0UploadsUploadIdGetRequest {
    uploadId: string;
}

export interface ListArtifactVersionsV0ArtifactsArtIdVersionsGetRequest {
    artId: string;
    cursor?: string | null;
    limit?: number;
}

export interface ListArtifactsV0ArtifactsGetRequest {
    prefix?: string;
    label?: Array<string>;
    fileType?: string | null;
    cursor?: string | null;
    limit?: number;
}

export interface ListEventsRouteV0EventsGetRequest {
    artId?: string | null;
    action?: string | null;
    since?: Date | null;
    before?: Date | null;
    cursor?: string | null;
    limit?: number;
}

export interface ListGrantsRouteV0GrantsGetRequest {
    resource: string;
    cursor?: string | null;
    limit?: number | null;
}

export interface ListProjectJobsV0ProjectsFldIdJobsGetRequest {
    fldId: string;
    status?: string | null;
    limit?: number;
    cursor?: string | null;
}

export interface ListSharesRouteV0SharesGetRequest {
    resource: string;
    cursor?: string | null;
    limit?: number | null;
}

export interface ListTrashRouteV0DrivesDriveIdTrashGetRequest {
    driveId: string;
    cursor?: string | null;
    limit?: number | null;
}

export interface LoginAuthLoginGetRequest {
    returnTo?: string | null;
}

export interface LogoutAuthLogoutPostRequest {
    csrf: string;
}

export interface MoveArtifactRouteV0ArtifactsArtIdMovePostRequest {
    artId: string;
    artifactMoveIn: ArtifactMoveIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface MoveFolderByIdV0FoldersFldIdMovePostRequest {
    fldId: string;
    folderMoveIn: FolderMoveIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface MoveFolderByPathV0FoldersPathMovePostRequest {
    path: string;
    folderMoveIn: FolderMoveIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface PatchArtifactRouteV0ArtifactsArtIdPatchRequest {
    artId: string;
    artifactPatchIn: ArtifactPatchIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface PatchFolderByIdV0FoldersFldIdPatchRequest {
    fldId: string;
    folderPatchIn: FolderPatchIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface PatchFolderByPathV0FoldersPathPatchRequest {
    path: string;
    folderPatchIn: FolderPatchIn;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface PatchGrantRouteV0GrantsGrnIdPatchRequest {
    grnId: string;
    grantPatchIn: GrantPatchIn;
    xAgentdriveActor?: string | null;
}

export interface PostDescribeV0QueryDescribePostRequest {
    describeIn: DescribeIn;
}

export interface PostLookupValuesV0QueryLookupValuesPostRequest {
    lookupValuesIn: LookupValuesIn;
}

export interface PostQueryV0QueryPostRequest {
    queryIn: QueryIn;
}

export interface PutArtifactV0ArtifactsPathPutRequest {
    path: string;
    contentType?: string;
    xAgentdriveLabels?: string | null;
    xAgentdriveMetadata?: string | null;
    xAgentdriveSource?: string | null;
    xAgentdriveActor?: string | null;
    xAgentdriveChangeSummary?: string | null;
    xAgentdriveChecksum?: string | null;
    contentMd5?: string | null;
    ifMatch?: string | null;
    ifNoneMatch?: string | null;
}

export interface PutProjectV0ProjectsFldIdPutRequest {
    fldId: string;
    projectConfigIn: ProjectConfigIn;
}

export interface RedeemShareSShareKeyGetRequest {
    shareKey: string;
}

export interface RedeemShareWithPasswordSShareKeyPostRequest {
    shareKey: string;
    password?: string;
}

export interface RestoreArtifactV0ArtifactsArtIdRestorePostRequest {
    artId: string;
    rename?: string | null;
    overwrite?: boolean;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface RestoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequest {
    artId: string;
    versionNumber: number;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface RestoreDriveRouteV0DrivesDriveIdRestorePostRequest {
    driveId: string;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface RestoreFolderByIdV0FoldersFldIdRestorePostRequest {
    fldId: string;
    xAgentdriveActor?: string | null;
    ifMatch?: string | null;
}

export interface RotateShareRouteV0SharesShrIdRotatePostRequest {
    shrId: string;
    xAgentdriveActor?: string | null;
}

export interface SearchV0SearchGetRequest {
    q: string;
    label?: Array<string>;
    fileType?: string | null;
    prefix?: string | null;
    updatedAfter?: Date | null;
    updatedBefore?: Date | null;
    limit?: number;
}

export interface ViewArtifactHeadAArtIdHeadGetRequest {
    artId: string;
}

export interface ViewArtifactVersionVArtIdVersionGetRequest {
    artId: string;
    version: number;
    raw?: number;
    download?: number;
}

export interface ViewFileDriveIdPathGetRequest {
    driveId: string;
    path: string;
    raw?: number;
    download?: number;
}

export interface ViewPermalinkArtifactAArtIdGetRequest {
    artId: string;
}

export interface ViewPermalinkFolderFFldIdGetRequest {
    fldId: string;
}

/**
 *
 */
export class DefaultApi extends runtime.BaseAPI {

    /**
     * Creates request options for abortUploadV0UploadsUploadIdDelete without sending the request
     */
    async abortUploadV0UploadsUploadIdDeleteRequestOpts(requestParameters: AbortUploadV0UploadsUploadIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['uploadId'] == null) {
            throw new runtime.RequiredError(
                'uploadId',
                'Required parameter "uploadId" was null or undefined when calling abortUploadV0UploadsUploadIdDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/uploads/{upload_id}`;
        urlPath = urlPath.replace('{upload_id}', encodeURIComponent(String(requestParameters['uploadId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Release an open upload session: return its reserved quota to the drive and mark it aborted. Idempotent — aborting an already-aborted or already-expired session succeeds with `released_bytes: 0`. A committed session cannot be aborted (409 ALREADY_COMMITTED). No write budget is charged — this frees resources rather than consuming them.
     * Abort a large (direct-to-GCS) upload session
     */
    async abortUploadV0UploadsUploadIdDeleteRaw(requestParameters: AbortUploadV0UploadsUploadIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UploadAbortOut>> {
        const requestOptions = await this.abortUploadV0UploadsUploadIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UploadAbortOutFromJSON(jsonValue));
    }

    /**
     * Release an open upload session: return its reserved quota to the drive and mark it aborted. Idempotent — aborting an already-aborted or already-expired session succeeds with `released_bytes: 0`. A committed session cannot be aborted (409 ALREADY_COMMITTED). No write budget is charged — this frees resources rather than consuming them.
     * Abort a large (direct-to-GCS) upload session
     */
    async abortUploadV0UploadsUploadIdDelete(requestParameters: AbortUploadV0UploadsUploadIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UploadAbortOut> {
        const response = await this.abortUploadV0UploadsUploadIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for beginUploadV0UploadsPost without sending the request
     */
    async beginUploadV0UploadsPostRequestOpts(requestParameters: BeginUploadV0UploadsPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['uploadBeginIn'] == null) {
            throw new runtime.RequiredError(
                'uploadBeginIn',
                'Required parameter "uploadBeginIn" was null or undefined when calling beginUploadV0UploadsPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/uploads`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UploadBeginInToJSON(requestParameters['uploadBeginIn']),
        };
    }

    /**
     * Reserve quota and open a resumable upload session for a file larger than the buffered-upload limit. Returns a `upload_url` to PUT the raw bytes to DIRECTLY (no Authorization header — the URL is the credential), then call `/v0/uploads/{upload_id}/commit`. All artifact decisions (path, labels, metadata, source, if_match) are frozen here.
     * Begin a large (direct-to-GCS) upload
     */
    async beginUploadV0UploadsPostRaw(requestParameters: BeginUploadV0UploadsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UploadBeginOut>> {
        const requestOptions = await this.beginUploadV0UploadsPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UploadBeginOutFromJSON(jsonValue));
    }

    /**
     * Reserve quota and open a resumable upload session for a file larger than the buffered-upload limit. Returns a `upload_url` to PUT the raw bytes to DIRECTLY (no Authorization header — the URL is the credential), then call `/v0/uploads/{upload_id}/commit`. All artifact decisions (path, labels, metadata, source, if_match) are frozen here.
     * Begin a large (direct-to-GCS) upload
     */
    async beginUploadV0UploadsPost(requestParameters: BeginUploadV0UploadsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UploadBeginOut> {
        const response = await this.beginUploadV0UploadsPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for callbackAuthCallbackGet without sending the request
     */
    async callbackAuthCallbackGetRequestOpts(requestParameters: CallbackAuthCallbackGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['code'] != null) {
            queryParameters['code'] = requestParameters['code'];
        }

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['error'] != null) {
            queryParameters['error'] = requestParameters['error'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/auth/callback`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Complete a sign-in.  Handles the auth provider\'s OAuth callback and shapes failures into user-readable errors:   * an invalid or expired login flow — LOGIN_FLOW_INVALID (400);   * an invalid or already-used authorization code — AUTH_CODE_INVALID (400);   * the upstream auth provider being unavailable — WORKOS_UNAVAILABLE (502),     returned with Retry-After.
     * Callback
     */
    async callbackAuthCallbackGetRaw(requestParameters: CallbackAuthCallbackGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const requestOptions = await this.callbackAuthCallbackGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Complete a sign-in.  Handles the auth provider\'s OAuth callback and shapes failures into user-readable errors:   * an invalid or expired login flow — LOGIN_FLOW_INVALID (400);   * an invalid or already-used authorization code — AUTH_CODE_INVALID (400);   * the upstream auth provider being unavailable — WORKOS_UNAVAILABLE (502),     returned with Retry-After.
     * Callback
     */
    async callbackAuthCallbackGet(requestParameters: CallbackAuthCallbackGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.callbackAuthCallbackGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for cancelJobV0JobsJobIdCancelPost without sending the request
     */
    async cancelJobV0JobsJobIdCancelPostRequestOpts(requestParameters: CancelJobV0JobsJobIdCancelPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling cancelJobV0JobsJobIdCancelPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/jobs/{job_id}/cancel`;
        urlPath = urlPath.replace('{job_id}', encodeURIComponent(String(requestParameters['jobId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Cancel a queued/running job
     */
    async cancelJobV0JobsJobIdCancelPostRaw(requestParameters: CancelJobV0JobsJobIdCancelPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileJobOut>> {
        const requestOptions = await this.cancelJobV0JobsJobIdCancelPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileJobOutFromJSON(jsonValue));
    }

    /**
     * Cancel a queued/running job
     */
    async cancelJobV0JobsJobIdCancelPost(requestParameters: CancelJobV0JobsJobIdCancelPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileJobOut> {
        const response = await this.cancelJobV0JobsJobIdCancelPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for commitUploadV0UploadsUploadIdCommitPost without sending the request
     */
    async commitUploadV0UploadsUploadIdCommitPostRequestOpts(requestParameters: CommitUploadV0UploadsUploadIdCommitPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['uploadId'] == null) {
            throw new runtime.RequiredError(
                'uploadId',
                'Required parameter "uploadId" was null or undefined when calling commitUploadV0UploadsUploadIdCommitPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/uploads/{upload_id}/commit`;
        urlPath = urlPath.replace('{upload_id}', encodeURIComponent(String(requestParameters['uploadId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Finalize the upload begun at `/v0/uploads`: AgentDrive verifies the object that landed in GCS (size + checksum) and creates the artifact. Idempotent — a retry after a successful commit returns the same artifact. The write budget is charged when the upload session is created; commit retries are not charged again.
     * Commit a large (direct-to-GCS) upload
     */
    async commitUploadV0UploadsUploadIdCommitPostRaw(requestParameters: CommitUploadV0UploadsUploadIdCommitPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.commitUploadV0UploadsUploadIdCommitPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Finalize the upload begun at `/v0/uploads`: AgentDrive verifies the object that landed in GCS (size + checksum) and creates the artifact. Idempotent — a retry after a successful commit returns the same artifact. The write budget is charged when the upload session is created; commit retries are not charged again.
     * Commit a large (direct-to-GCS) upload
     */
    async commitUploadV0UploadsUploadIdCommitPost(requestParameters: CommitUploadV0UploadsUploadIdCommitPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.commitUploadV0UploadsUploadIdCommitPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for copyArtifactRouteV0ArtifactsArtIdCopyPost without sending the request
     */
    async copyArtifactRouteV0ArtifactsArtIdCopyPostRequestOpts(requestParameters: CopyArtifactRouteV0ArtifactsArtIdCopyPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling copyArtifactRouteV0ArtifactsArtIdCopyPost().'
            );
        }

        if (requestParameters['copyIn'] == null) {
            throw new runtime.RequiredError(
                'copyIn',
                'Required parameter "copyIn" was null or undefined when calling copyArtifactRouteV0ArtifactsArtIdCopyPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['if-none-match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/copy`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CopyInToJSON(requestParameters['copyIn']),
        };
    }

    /**
     * Create a new artifact at `path` whose bytes are identical to the source artifact\'s. The copy reuses the source\'s CAS object (zero new storage) but gets a fresh `art_…` ID, a fresh version 1, and — by default — `source.refs = [{type: \'artifact\', id: \'<source>\'}]` so provenance is preserved.  Quota: the copy\'s `size_bytes` is added to the drive\'s `storage_bytes` even though physical bytes are shared.  Source-version pin: pass `from_generation` in the body to require the source\'s current content generation (`version_number`) to equal it (→ 412 SOURCE_VERSION_MISMATCH); a concurrent source *metadata* edit does NOT fail the copy. Destination create-only: `If-None-Match: *` returns 412 CREATE_CONFLICT (instead of 409 PATH_CONFLICT) when the target path is occupied.  Returns 409 PATH_CONFLICT if the target path is already taken; 413 STORAGE_QUOTA_EXCEEDED if the copy would push the drive over its limit.
     * Duplicate an artifact to a new path (CAS-shared, new ID)
     */
    async copyArtifactRouteV0ArtifactsArtIdCopyPostRaw(requestParameters: CopyArtifactRouteV0ArtifactsArtIdCopyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.copyArtifactRouteV0ArtifactsArtIdCopyPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Create a new artifact at `path` whose bytes are identical to the source artifact\'s. The copy reuses the source\'s CAS object (zero new storage) but gets a fresh `art_…` ID, a fresh version 1, and — by default — `source.refs = [{type: \'artifact\', id: \'<source>\'}]` so provenance is preserved.  Quota: the copy\'s `size_bytes` is added to the drive\'s `storage_bytes` even though physical bytes are shared.  Source-version pin: pass `from_generation` in the body to require the source\'s current content generation (`version_number`) to equal it (→ 412 SOURCE_VERSION_MISMATCH); a concurrent source *metadata* edit does NOT fail the copy. Destination create-only: `If-None-Match: *` returns 412 CREATE_CONFLICT (instead of 409 PATH_CONFLICT) when the target path is occupied.  Returns 409 PATH_CONFLICT if the target path is already taken; 413 STORAGE_QUOTA_EXCEEDED if the copy would push the drive over its limit.
     * Duplicate an artifact to a new path (CAS-shared, new ID)
     */
    async copyArtifactRouteV0ArtifactsArtIdCopyPost(requestParameters: CopyArtifactRouteV0ArtifactsArtIdCopyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.copyArtifactRouteV0ArtifactsArtIdCopyPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for copyFolderByIdV0FoldersFldIdCopyPost without sending the request
     */
    async copyFolderByIdV0FoldersFldIdCopyPostRequestOpts(requestParameters: CopyFolderByIdV0FoldersFldIdCopyPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling copyFolderByIdV0FoldersFldIdCopyPost().'
            );
        }

        if (requestParameters['folderCopyIn'] == null) {
            throw new runtime.RequiredError(
                'folderCopyIn',
                'Required parameter "folderCopyIn" was null or undefined when calling copyFolderByIdV0FoldersFldIdCopyPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['if-none-match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}/copy`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FolderCopyInToJSON(requestParameters['folderCopyIn']),
        };
    }

    /**
     * Clone the folder identified by URL id — and every descendant folder + artifact — under the body\'s `path` (canonical, trailing slash). Each copied artifact reuses the source\'s CAS object (zero new storage) but gets a fresh `art_…` ID, a fresh version 1, and `source.refs = [{type: \'artifact\', id: \'<source>\'}]` provenance. The new folder gets a fresh `fld_…` ID and the source\'s description.  The entire subtree is copied in a SINGLE transaction — either every row lands or none does.  Quota: each copy\'s `size_bytes` counts against the drive\'s `storage_bytes` even though physical bytes are shared.  Source-version pin: pass `from_metageneration` in the body to require the source folder\'s current `metageneration` to equal it (→ 412 SOURCE_VERSION_MISMATCH). Destination create-only: `If-None-Match: *` returns 412 CREATE_CONFLICT (instead of 409 FOLDER_PATH_CONFLICT) when the destination folder is occupied.  Returns 409 `FOLDER_PATH_CONFLICT` if the destination collides with a live folder or artifact; 400 `FOLDER_PATH_INVALID` if `path` is non-canonical; 413 `SUBTREE_TOO_LARGE` if the source holds more than 5000 artifacts; 413 `STORAGE_QUOTA_EXCEEDED` if the copy would push the drive over its limit.
     * Duplicate a folder subtree to a new path (CAS-shared, new IDs)
     */
    async copyFolderByIdV0FoldersFldIdCopyPostRaw(requestParameters: CopyFolderByIdV0FoldersFldIdCopyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderCopyOut>> {
        const requestOptions = await this.copyFolderByIdV0FoldersFldIdCopyPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderCopyOutFromJSON(jsonValue));
    }

    /**
     * Clone the folder identified by URL id — and every descendant folder + artifact — under the body\'s `path` (canonical, trailing slash). Each copied artifact reuses the source\'s CAS object (zero new storage) but gets a fresh `art_…` ID, a fresh version 1, and `source.refs = [{type: \'artifact\', id: \'<source>\'}]` provenance. The new folder gets a fresh `fld_…` ID and the source\'s description.  The entire subtree is copied in a SINGLE transaction — either every row lands or none does.  Quota: each copy\'s `size_bytes` counts against the drive\'s `storage_bytes` even though physical bytes are shared.  Source-version pin: pass `from_metageneration` in the body to require the source folder\'s current `metageneration` to equal it (→ 412 SOURCE_VERSION_MISMATCH). Destination create-only: `If-None-Match: *` returns 412 CREATE_CONFLICT (instead of 409 FOLDER_PATH_CONFLICT) when the destination folder is occupied.  Returns 409 `FOLDER_PATH_CONFLICT` if the destination collides with a live folder or artifact; 400 `FOLDER_PATH_INVALID` if `path` is non-canonical; 413 `SUBTREE_TOO_LARGE` if the source holds more than 5000 artifacts; 413 `STORAGE_QUOTA_EXCEEDED` if the copy would push the drive over its limit.
     * Duplicate a folder subtree to a new path (CAS-shared, new IDs)
     */
    async copyFolderByIdV0FoldersFldIdCopyPost(requestParameters: CopyFolderByIdV0FoldersFldIdCopyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderCopyOut> {
        const response = await this.copyFolderByIdV0FoldersFldIdCopyPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createFolderByPathV0FoldersPathPut without sending the request
     */
    async createFolderByPathV0FoldersPathPutRequestOpts(requestParameters: CreateFolderByPathV0FoldersPathPutRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling createFolderByPathV0FoldersPathPut().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['if-none-match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: FolderCreateInToJSON(requestParameters['folderCreateIn']),
        };
    }

    /**
     * Create a folder at the URL path. Idempotent create-at-known-URI (mirrors `PUT /v0/artifacts/{path}`) — a second call for the same live path returns the existing row unchanged (metadata updates require PATCH). Returns 201 on create, 200 when the folder already exists.  Send `If-None-Match: *` to make it strictly create-only: an existing folder then returns 412 CREATE_CONFLICT instead of the idempotent 200.  Returns 409 `FOLDER_PATH_CONFLICT` if a live artifact occupies the file form of the path (e.g. mkdir `/foo/` when an artifact lives at `/foo`).
     * Create a folder (idempotent)
     */
    async createFolderByPathV0FoldersPathPutRaw(requestParameters: CreateFolderByPathV0FoldersPathPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.createFolderByPathV0FoldersPathPutRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Create a folder at the URL path. Idempotent create-at-known-URI (mirrors `PUT /v0/artifacts/{path}`) — a second call for the same live path returns the existing row unchanged (metadata updates require PATCH). Returns 201 on create, 200 when the folder already exists.  Send `If-None-Match: *` to make it strictly create-only: an existing folder then returns 412 CREATE_CONFLICT instead of the idempotent 200.  Returns 409 `FOLDER_PATH_CONFLICT` if a live artifact occupies the file form of the path (e.g. mkdir `/foo/` when an artifact lives at `/foo`).
     * Create a folder (idempotent)
     */
    async createFolderByPathV0FoldersPathPut(requestParameters: CreateFolderByPathV0FoldersPathPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.createFolderByPathV0FoldersPathPutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createGrantRouteV0GrantsPost without sending the request
     */
    async createGrantRouteV0GrantsPostRequestOpts(requestParameters: CreateGrantRouteV0GrantsPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['grantCreateIn'] == null) {
            throw new runtime.RequiredError(
                'grantCreateIn',
                'Required parameter "grantCreateIn" was null or undefined when calling createGrantRouteV0GrantsPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/grants`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GrantCreateInToJSON(requestParameters['grantCreateIn']),
        };
    }

    /**
     * Create (or fetch) a per-principal grant on a resource
     */
    async createGrantRouteV0GrantsPostRaw(requestParameters: CreateGrantRouteV0GrantsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        const requestOptions = await this.createGrantRouteV0GrantsPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * Create (or fetch) a per-principal grant on a resource
     */
    async createGrantRouteV0GrantsPost(requestParameters: CreateGrantRouteV0GrantsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.createGrantRouteV0GrantsPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createShareRouteV0SharesPost without sending the request
     */
    async createShareRouteV0SharesPostRequestOpts(requestParameters: CreateShareRouteV0SharesPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shareCreateIn'] == null) {
            throw new runtime.RequiredError(
                'shareCreateIn',
                'Required parameter "shareCreateIn" was null or undefined when calling createShareRouteV0SharesPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/shares`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ShareCreateInToJSON(requestParameters['shareCreateIn']),
        };
    }

    /**
     * Mint a share link (returns the share_key once)
     */
    async createShareRouteV0SharesPostRaw(requestParameters: CreateShareRouteV0SharesPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareMintOut>> {
        const requestOptions = await this.createShareRouteV0SharesPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareMintOutFromJSON(jsonValue));
    }

    /**
     * Mint a share link (returns the share_key once)
     */
    async createShareRouteV0SharesPost(requestParameters: CreateShareRouteV0SharesPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareMintOut> {
        const response = await this.createShareRouteV0SharesPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteArtifactByIdRouteV0ArtifactsArtIdDelete without sending the request
     */
    async deleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequestOpts(requestParameters: DeleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling deleteArtifactByIdRouteV0ArtifactsArtIdDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft-delete the artifact with this `art_…` ID. Same semantics + response shape as the path-based `DELETE /v0/artifacts/{path}` (reversible until the GC cron hard-deletes at `purge_at`; `restore_url` points at the by-id restore), but keys on the immutable id so a concurrent rename can\'t change the target.  Returns 404 ARTIFACT_NOT_FOUND if no live artifact has this id; 403 WIKI_RESERVED for `_wiki/` artifacts (system-managed); 412 if `If-Match` doesn\'t match the current version. Declared before the `{path:path}` family so the id convertor wins for DELETEs.
     * Soft-delete an artifact by its stable ID
     */
    async deleteArtifactByIdRouteV0ArtifactsArtIdDeleteRaw(requestParameters: DeleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactDeleteOut>> {
        const requestOptions = await this.deleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactDeleteOutFromJSON(jsonValue));
    }

    /**
     * Soft-delete the artifact with this `art_…` ID. Same semantics + response shape as the path-based `DELETE /v0/artifacts/{path}` (reversible until the GC cron hard-deletes at `purge_at`; `restore_url` points at the by-id restore), but keys on the immutable id so a concurrent rename can\'t change the target.  Returns 404 ARTIFACT_NOT_FOUND if no live artifact has this id; 403 WIKI_RESERVED for `_wiki/` artifacts (system-managed); 412 if `If-Match` doesn\'t match the current version. Declared before the `{path:path}` family so the id convertor wins for DELETEs.
     * Soft-delete an artifact by its stable ID
     */
    async deleteArtifactByIdRouteV0ArtifactsArtIdDelete(requestParameters: DeleteArtifactByIdRouteV0ArtifactsArtIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactDeleteOut> {
        const response = await this.deleteArtifactByIdRouteV0ArtifactsArtIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteArtifactV0ArtifactsPathDelete without sending the request
     */
    async deleteArtifactV0ArtifactsPathDeleteRequestOpts(requestParameters: DeleteArtifactV0ArtifactsPathDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling deleteArtifactV0ArtifactsPathDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft-delete the artifact at the given path.  A delete WITHOUT an `If-Match` precondition is last-writer-wins and will silently remove a concurrently-modified artifact.
     * Delete Artifact
     */
    async deleteArtifactV0ArtifactsPathDeleteRaw(requestParameters: DeleteArtifactV0ArtifactsPathDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactDeleteOut>> {
        const requestOptions = await this.deleteArtifactV0ArtifactsPathDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactDeleteOutFromJSON(jsonValue));
    }

    /**
     * Soft-delete the artifact at the given path.  A delete WITHOUT an `If-Match` precondition is last-writer-wins and will silently remove a concurrently-modified artifact.
     * Delete Artifact
     */
    async deleteArtifactV0ArtifactsPathDelete(requestParameters: DeleteArtifactV0ArtifactsPathDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactDeleteOut> {
        const response = await this.deleteArtifactV0ArtifactsPathDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteDriveRouteV0DrivesDriveIdDelete without sending the request
     */
    async deleteDriveRouteV0DrivesDriveIdDeleteRequestOpts(requestParameters: DeleteDriveRouteV0DrivesDriveIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling deleteDriveRouteV0DrivesDriveIdDelete().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['confirm'] != null) {
            queryParameters['confirm'] = requestParameters['confirm'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Mark the drive for cleanup. All tenant data (artifacts, versions, wiki, embeddings, events) is hidden via the `live_*` views and CASCADE-removed by the GC cleanup cron at `purge_at`. Restore via `POST /v0/drives/{id}/restore` while the row is still in trash. The path-param `drive_id` MUST match the authenticated drive.  Accepts either an `ad_live_` per-drive key (deletes that key\'s drive) or an `ad_user_` user token selecting an owned drive (workspaces-design §5.3); a `read`-scope user token is rejected with 403 `INSUFFICIENT_SCOPE`. **Guard (§8):** a workspace must retain at least one live drive — deleting the workspace\'s last live drive returns 409 `LAST_DRIVE`.  **Explicit confirmation required:** pass `?confirm=DELETE` or the request is rejected with 400 `CONFIRM_REQUIRED`. Tenant-level deletion is the largest-blast-radius operation on the API; the static token forces a deliberate act (soft-delete still gives a restore window on top).  **Optimistic concurrency:** send `If-Match` with the drive\'s composite ETag (`\"<drv_id>.0.<metageneration>\"`, from a drive read) to make the delete conditional — a stale token returns 412 PRECONDITION_FAILED. A delete WITHOUT an `If-Match` precondition is last-writer-wins and will silently trash a concurrently-modified drive.
     * Soft-delete a drive
     */
    async deleteDriveRouteV0DrivesDriveIdDeleteRaw(requestParameters: DeleteDriveRouteV0DrivesDriveIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveDeleteOut>> {
        const requestOptions = await this.deleteDriveRouteV0DrivesDriveIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveDeleteOutFromJSON(jsonValue));
    }

    /**
     * Mark the drive for cleanup. All tenant data (artifacts, versions, wiki, embeddings, events) is hidden via the `live_*` views and CASCADE-removed by the GC cleanup cron at `purge_at`. Restore via `POST /v0/drives/{id}/restore` while the row is still in trash. The path-param `drive_id` MUST match the authenticated drive.  Accepts either an `ad_live_` per-drive key (deletes that key\'s drive) or an `ad_user_` user token selecting an owned drive (workspaces-design §5.3); a `read`-scope user token is rejected with 403 `INSUFFICIENT_SCOPE`. **Guard (§8):** a workspace must retain at least one live drive — deleting the workspace\'s last live drive returns 409 `LAST_DRIVE`.  **Explicit confirmation required:** pass `?confirm=DELETE` or the request is rejected with 400 `CONFIRM_REQUIRED`. Tenant-level deletion is the largest-blast-radius operation on the API; the static token forces a deliberate act (soft-delete still gives a restore window on top).  **Optimistic concurrency:** send `If-Match` with the drive\'s composite ETag (`\"<drv_id>.0.<metageneration>\"`, from a drive read) to make the delete conditional — a stale token returns 412 PRECONDITION_FAILED. A delete WITHOUT an `If-Match` precondition is last-writer-wins and will silently trash a concurrently-modified drive.
     * Soft-delete a drive
     */
    async deleteDriveRouteV0DrivesDriveIdDelete(requestParameters: DeleteDriveRouteV0DrivesDriveIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveDeleteOut> {
        const response = await this.deleteDriveRouteV0DrivesDriveIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteFolderByIdV0FoldersFldIdDelete without sending the request
     */
    async deleteFolderByIdV0FoldersFldIdDeleteRequestOpts(requestParameters: DeleteFolderByIdV0FoldersFldIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling deleteFolderByIdV0FoldersFldIdDelete().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['recursive'] != null) {
            queryParameters['recursive'] = requestParameters['recursive'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft-delete a folder by stable ID (cascade with ?recursive=true)
     */
    async deleteFolderByIdV0FoldersFldIdDeleteRaw(requestParameters: DeleteFolderByIdV0FoldersFldIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderDeleteOut>> {
        const requestOptions = await this.deleteFolderByIdV0FoldersFldIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderDeleteOutFromJSON(jsonValue));
    }

    /**
     * Soft-delete a folder by stable ID (cascade with ?recursive=true)
     */
    async deleteFolderByIdV0FoldersFldIdDelete(requestParameters: DeleteFolderByIdV0FoldersFldIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderDeleteOut> {
        const response = await this.deleteFolderByIdV0FoldersFldIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteFolderByPathV0FoldersPathDelete without sending the request
     */
    async deleteFolderByPathV0FoldersPathDeleteRequestOpts(requestParameters: DeleteFolderByPathV0FoldersPathDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling deleteFolderByPathV0FoldersPathDelete().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['recursive'] != null) {
            queryParameters['recursive'] = requestParameters['recursive'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft-delete the folder. Refuses if the folder has live descendants unless `?recursive=true` is set, in which case ALL descendant folders + artifacts are soft-deleted in the same transaction.  Returns 409 `FOLDER_RECURSIVE_REQUIRED` (with descendant counts in `colliding_path`) when recursion is needed but the flag isn\'t set. Retention window is frozen on `purge_at` per deletion-design.md §5.1; mid-retention tier changes don\'t shift it.
     * Soft-delete a folder (cascade with ?recursive=true)
     */
    async deleteFolderByPathV0FoldersPathDeleteRaw(requestParameters: DeleteFolderByPathV0FoldersPathDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderDeleteOut>> {
        const requestOptions = await this.deleteFolderByPathV0FoldersPathDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderDeleteOutFromJSON(jsonValue));
    }

    /**
     * Soft-delete the folder. Refuses if the folder has live descendants unless `?recursive=true` is set, in which case ALL descendant folders + artifacts are soft-deleted in the same transaction.  Returns 409 `FOLDER_RECURSIVE_REQUIRED` (with descendant counts in `colliding_path`) when recursion is needed but the flag isn\'t set. Retention window is frozen on `purge_at` per deletion-design.md §5.1; mid-retention tier changes don\'t shift it.
     * Soft-delete a folder (cascade with ?recursive=true)
     */
    async deleteFolderByPathV0FoldersPathDelete(requestParameters: DeleteFolderByPathV0FoldersPathDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderDeleteOut> {
        const response = await this.deleteFolderByPathV0FoldersPathDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteGrantRouteV0GrantsGrnIdDelete without sending the request
     */
    async deleteGrantRouteV0GrantsGrnIdDeleteRequestOpts(requestParameters: DeleteGrantRouteV0GrantsGrnIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['grnId'] == null) {
            throw new runtime.RequiredError(
                'grnId',
                'Required parameter "grnId" was null or undefined when calling deleteGrantRouteV0GrantsGrnIdDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/grants/{grn_id}`;
        urlPath = urlPath.replace('{grn_id}', encodeURIComponent(String(requestParameters['grnId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke a grant (can_manage, or self-revoke own grant)
     */
    async deleteGrantRouteV0GrantsGrnIdDeleteRaw(requestParameters: DeleteGrantRouteV0GrantsGrnIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevokeOut>> {
        const requestOptions = await this.deleteGrantRouteV0GrantsGrnIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevokeOutFromJSON(jsonValue));
    }

    /**
     * Revoke a grant (can_manage, or self-revoke own grant)
     */
    async deleteGrantRouteV0GrantsGrnIdDelete(requestParameters: DeleteGrantRouteV0GrantsGrnIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevokeOut> {
        const response = await this.deleteGrantRouteV0GrantsGrnIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteShareRouteV0SharesShrIdDelete without sending the request
     */
    async deleteShareRouteV0SharesShrIdDeleteRequestOpts(requestParameters: DeleteShareRouteV0SharesShrIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shrId'] == null) {
            throw new runtime.RequiredError(
                'shrId',
                'Required parameter "shrId" was null or undefined when calling deleteShareRouteV0SharesShrIdDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/shares/{shr_id}`;
        urlPath = urlPath.replace('{shr_id}', encodeURIComponent(String(requestParameters['shrId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke a share link (requires can_manage)
     */
    async deleteShareRouteV0SharesShrIdDeleteRaw(requestParameters: DeleteShareRouteV0SharesShrIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevokeOut>> {
        const requestOptions = await this.deleteShareRouteV0SharesShrIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevokeOutFromJSON(jsonValue));
    }

    /**
     * Revoke a share link (requires can_manage)
     */
    async deleteShareRouteV0SharesShrIdDelete(requestParameters: DeleteShareRouteV0SharesShrIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevokeOut> {
        const response = await this.deleteShareRouteV0SharesShrIdDeleteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadArtifactByIdV0ArtifactsArtIdDownloadGet without sending the request
     */
    async downloadArtifactByIdV0ArtifactsArtIdDownloadGetRequestOpts(requestParameters: DownloadArtifactByIdV0ArtifactsArtIdDownloadGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling downloadArtifactByIdV0ArtifactsArtIdDownloadGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/download`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Stream the artifact bytes by stable ID (never rendered HTML)
     */
    async downloadArtifactByIdV0ArtifactsArtIdDownloadGetRaw(requestParameters: DownloadArtifactByIdV0ArtifactsArtIdDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.downloadArtifactByIdV0ArtifactsArtIdDownloadGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Stream the artifact bytes by stable ID (never rendered HTML)
     */
    async downloadArtifactByIdV0ArtifactsArtIdDownloadGet(requestParameters: DownloadArtifactByIdV0ArtifactsArtIdDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.downloadArtifactByIdV0ArtifactsArtIdDownloadGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadArtifactByPathV0ArtifactsPathDownloadGet without sending the request
     */
    async downloadArtifactByPathV0ArtifactsPathDownloadGetRequestOpts(requestParameters: DownloadArtifactByPathV0ArtifactsPathDownloadGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling downloadArtifactByPathV0ArtifactsPathDownloadGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{path}/download`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Same bytes-only machine surface as `/{art_id}/download` but resolves the artifact by path, so callers don\'t have to resolve path→id first. Applies the identical CSP `sandbox` + `nosniff` posture (never serves HTML inline as active content).
     * Stream the artifact bytes by path (never rendered HTML)
     */
    async downloadArtifactByPathV0ArtifactsPathDownloadGetRaw(requestParameters: DownloadArtifactByPathV0ArtifactsPathDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.downloadArtifactByPathV0ArtifactsPathDownloadGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Same bytes-only machine surface as `/{art_id}/download` but resolves the artifact by path, so callers don\'t have to resolve path→id first. Applies the identical CSP `sandbox` + `nosniff` posture (never serves HTML inline as active content).
     * Stream the artifact bytes by path (never rendered HTML)
     */
    async downloadArtifactByPathV0ArtifactsPathDownloadGet(requestParameters: DownloadArtifactByPathV0ArtifactsPathDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.downloadArtifactByPathV0ArtifactsPathDownloadGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGet without sending the request
     */
    async downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequestOpts(requestParameters: DownloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGet().'
            );
        }

        if (requestParameters['versionNumber'] == null) {
            throw new runtime.RequiredError(
                'versionNumber',
                'Required parameter "versionNumber" was null or undefined when calling downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/versions/{version_number}/download`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));
        urlPath = urlPath.replace('{version_number}', encodeURIComponent(String(requestParameters['versionNumber'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Stream bytes for a specific version (machine surface)
     */
    async downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRaw(requestParameters: DownloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Stream bytes for a specific version (machine surface)
     */
    async downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGet(requestParameters: DownloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.downloadArtifactVersionV0ArtifactsArtIdVersionsVersionNumberDownloadGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadUrlByIdV0ArtifactsArtIdDownloadUrlGet without sending the request
     */
    async downloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequestOpts(requestParameters: DownloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling downloadUrlByIdV0ArtifactsArtIdDownloadUrlGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/download-url`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a URL for the artifact\'s bytes. For large artifacts (>= the signed-download threshold) when signing is available, it\'s a short-lived **signed GCS URL** the client fetches directly (`direct:true`, `expires_at` set); otherwise the **proxy** `/download` URL (`direct:false`). Treat the URL as opaque. large-download-design.md §5.1.
     * Signed direct-from-GCS download URL by stable ID
     */
    async downloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRaw(requestParameters: DownloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DownloadUrlOut>> {
        const requestOptions = await this.downloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DownloadUrlOutFromJSON(jsonValue));
    }

    /**
     * Returns a URL for the artifact\'s bytes. For large artifacts (>= the signed-download threshold) when signing is available, it\'s a short-lived **signed GCS URL** the client fetches directly (`direct:true`, `expires_at` set); otherwise the **proxy** `/download` URL (`direct:false`). Treat the URL as opaque. large-download-design.md §5.1.
     * Signed direct-from-GCS download URL by stable ID
     */
    async downloadUrlByIdV0ArtifactsArtIdDownloadUrlGet(requestParameters: DownloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DownloadUrlOut> {
        const response = await this.downloadUrlByIdV0ArtifactsArtIdDownloadUrlGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadUrlByPathV0ArtifactsPathDownloadUrlGet without sending the request
     */
    async downloadUrlByPathV0ArtifactsPathDownloadUrlGetRequestOpts(requestParameters: DownloadUrlByPathV0ArtifactsPathDownloadUrlGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling downloadUrlByPathV0ArtifactsPathDownloadUrlGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{path}/download-url`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Same as `/{art_id}/download-url` but resolves the artifact by path. The returned proxy URL (when `direct:false`) still points at the by-id `/download` endpoint. large-download-design.md §5.1.
     * Signed direct-from-GCS download URL by path
     */
    async downloadUrlByPathV0ArtifactsPathDownloadUrlGetRaw(requestParameters: DownloadUrlByPathV0ArtifactsPathDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DownloadUrlOut>> {
        const requestOptions = await this.downloadUrlByPathV0ArtifactsPathDownloadUrlGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DownloadUrlOutFromJSON(jsonValue));
    }

    /**
     * Same as `/{art_id}/download-url` but resolves the artifact by path. The returned proxy URL (when `direct:false`) still points at the by-id `/download` endpoint. large-download-design.md §5.1.
     * Signed direct-from-GCS download URL by path
     */
    async downloadUrlByPathV0ArtifactsPathDownloadUrlGet(requestParameters: DownloadUrlByPathV0ArtifactsPathDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DownloadUrlOut> {
        const response = await this.downloadUrlByPathV0ArtifactsPathDownloadUrlGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGet without sending the request
     */
    async downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequestOpts(requestParameters: DownloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGet().'
            );
        }

        if (requestParameters['versionNumber'] == null) {
            throw new runtime.RequiredError(
                'versionNumber',
                'Required parameter "versionNumber" was null or undefined when calling downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/versions/{version_number}/download-url`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));
        urlPath = urlPath.replace('{version_number}', encodeURIComponent(String(requestParameters['versionNumber'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Same as `/{art_id}/download-url` but for a specific version\'s bytes (`direct:true` signed GCS URL when large + signing available, else the proxy `/versions/{n}/download` URL). large-download-design.md §5.1.
     * Signed direct-from-GCS download URL for a specific version
     */
    async downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRaw(requestParameters: DownloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DownloadUrlOut>> {
        const requestOptions = await this.downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DownloadUrlOutFromJSON(jsonValue));
    }

    /**
     * Same as `/{art_id}/download-url` but for a specific version\'s bytes (`direct:true` signed GCS URL when large + signing available, else the proxy `/versions/{n}/download` URL). large-download-design.md §5.1.
     * Signed direct-from-GCS download URL for a specific version
     */
    async downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGet(requestParameters: DownloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DownloadUrlOut> {
        const response = await this.downloadUrlVersionV0ArtifactsArtIdVersionsVersionNumberDownloadUrlGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for enqueueJobV0ProjectsFldIdJobsPost without sending the request
     */
    async enqueueJobV0ProjectsFldIdJobsPostRequestOpts(requestParameters: EnqueueJobV0ProjectsFldIdJobsPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling enqueueJobV0ProjectsFldIdJobsPost().'
            );
        }

        if (requestParameters['compileJobIn'] == null) {
            throw new runtime.RequiredError(
                'compileJobIn',
                'Required parameter "compileJobIn" was null or undefined when calling enqueueJobV0ProjectsFldIdJobsPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/projects/{fld_id}/jobs`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CompileJobInToJSON(requestParameters['compileJobIn']),
        };
    }

    /**
     * Enqueue a compile job for a project (folder)
     */
    async enqueueJobV0ProjectsFldIdJobsPostRaw(requestParameters: EnqueueJobV0ProjectsFldIdJobsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileJobOut>> {
        const requestOptions = await this.enqueueJobV0ProjectsFldIdJobsPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileJobOutFromJSON(jsonValue));
    }

    /**
     * Enqueue a compile job for a project (folder)
     */
    async enqueueJobV0ProjectsFldIdJobsPost(requestParameters: EnqueueJobV0ProjectsFldIdJobsPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileJobOut> {
        const response = await this.enqueueJobV0ProjectsFldIdJobsPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for extensionStartAuthExtensionStartGet without sending the request
     */
    async extensionStartAuthExtensionStartGetRequestOpts(requestParameters: ExtensionStartAuthExtensionStartGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['extId'] != null) {
            queryParameters['ext_id'] = requestParameters['extId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/auth/extension/start`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Begin a sign-in flow on behalf of a Chrome extension.  Provider follows AUTH_MODE (WorkOS AuthKit or the TokenCanopy hub), exactly like /auth/login. Stamps `for=ext` + `ext_id` into the signed OAuth state so the callback handler knows to render the extension handoff page instead of setting a session cookie.  Three short-circuits, all surface as actionable errors:   * EXTENSION_AUTH_DISABLED (503): kill switch flipped off.   * UNKNOWN_EXTENSION (400): `ext_id` not on the allow-list.   * Missing `ext_id` query string (400 INVALID_REQUEST).
     * Extension Start
     */
    async extensionStartAuthExtensionStartGetRaw(requestParameters: ExtensionStartAuthExtensionStartGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.extensionStartAuthExtensionStartGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Begin a sign-in flow on behalf of a Chrome extension.  Provider follows AUTH_MODE (WorkOS AuthKit or the TokenCanopy hub), exactly like /auth/login. Stamps `for=ext` + `ext_id` into the signed OAuth state so the callback handler knows to render the extension handoff page instead of setting a session cookie.  Three short-circuits, all surface as actionable errors:   * EXTENSION_AUTH_DISABLED (503): kill switch flipped off.   * UNKNOWN_EXTENSION (400): `ext_id` not on the allow-list.   * Missing `ext_id` query string (400 INVALID_REQUEST).
     * Extension Start
     */
    async extensionStartAuthExtensionStartGet(requestParameters: ExtensionStartAuthExtensionStartGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.extensionStartAuthExtensionStartGetRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for findV0FindGet without sending the request
     */
    async findV0FindGetRequestOpts(requestParameters: FindV0FindGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['q'] == null) {
            throw new runtime.RequiredError(
                'q',
                'Required parameter "q" was null or undefined when calling findV0FindGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['mode'] != null) {
            queryParameters['mode'] = requestParameters['mode'];
        }

        if (requestParameters['label'] != null) {
            queryParameters['label'] = requestParameters['label'];
        }

        if (requestParameters['fileType'] != null) {
            queryParameters['file_type'] = requestParameters['fileType'];
        }

        if (requestParameters['prefix'] != null) {
            queryParameters['prefix'] = requestParameters['prefix'];
        }

        if (requestParameters['modality'] != null) {
            queryParameters['modality'] = requestParameters['modality'];
        }

        if (requestParameters['updatedAfter'] != null) {
            queryParameters['updated_after'] = (requestParameters['updatedAfter'] as any).toISOString();
        }

        if (requestParameters['updatedBefore'] != null) {
            queryParameters['updated_before'] = (requestParameters['updatedBefore'] as any).toISOString();
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/find`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Passage-level chunk RAG over `embed_chunks`. Lexical (`chunk_tsv`, GIN) + semantic (HNSW over `embedding`) are run in parallel and fused via Reciprocal Rank Fusion (k=60). Unlike `/v0/search`, which only sees the first ~16 KB preview of each artifact, `/v0/find` reaches the full file body.  **Modes:** - `hybrid` (default) — lexical + semantic, RRF-fused. - `lexical` — `chunk_tsv` only. Best for exact tokens, identifiers, code snippets. - `semantic` — embedding only. Best for conceptual queries where the surface terms differ from the query phrasing.  **Granularity:** results are passages, not files. A long document with multiple matching regions returns multiple hits with distinct `ord` values; consecutive `ord`s overlap by ~400 tokens. Dedupe by `art_id` if you want one row per file.  **Span citations:** `char_start`/`char_end` for text & code, `page_start`/`page_end` for PDFs, `time_start_ms`/`time_end_ms` for audio & video. Only the modality-relevant pair is populated.  **Filters:** `label`, `file_type`, `prefix`, `modality` (repeatable), `updated_after` / `updated_before` (RFC 3339 timestamps, inclusive bounds on `updated_at`, applied to both legs).  **Wiki coverage:** `/v0/find` excludes `_wiki/` paths by default and — importantly — does NOT cover them even when the caller passes `prefix=_wiki/...`. Wiki pages are not embedded by the pipeline (they\'re system-generated output, not user input), so `embed_chunks` has no rows for them and the join returns empty. Use `wiki_search` (or `list`/`grep` with a `_wiki/` prefix) for the wiki layer.  **Embedding availability:** when `GEMINI_API_KEY` is not configured, `mode=semantic` returns 503; `mode=hybrid` logs a warning and falls back to lexical-only; `mode=lexical` is unaffected.
     * Hybrid passage retrieval over the full file body
     */
    async findV0FindGetRaw(requestParameters: FindV0FindGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FindPage>> {
        const requestOptions = await this.findV0FindGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FindPageFromJSON(jsonValue));
    }

    /**
     * Passage-level chunk RAG over `embed_chunks`. Lexical (`chunk_tsv`, GIN) + semantic (HNSW over `embedding`) are run in parallel and fused via Reciprocal Rank Fusion (k=60). Unlike `/v0/search`, which only sees the first ~16 KB preview of each artifact, `/v0/find` reaches the full file body.  **Modes:** - `hybrid` (default) — lexical + semantic, RRF-fused. - `lexical` — `chunk_tsv` only. Best for exact tokens, identifiers, code snippets. - `semantic` — embedding only. Best for conceptual queries where the surface terms differ from the query phrasing.  **Granularity:** results are passages, not files. A long document with multiple matching regions returns multiple hits with distinct `ord` values; consecutive `ord`s overlap by ~400 tokens. Dedupe by `art_id` if you want one row per file.  **Span citations:** `char_start`/`char_end` for text & code, `page_start`/`page_end` for PDFs, `time_start_ms`/`time_end_ms` for audio & video. Only the modality-relevant pair is populated.  **Filters:** `label`, `file_type`, `prefix`, `modality` (repeatable), `updated_after` / `updated_before` (RFC 3339 timestamps, inclusive bounds on `updated_at`, applied to both legs).  **Wiki coverage:** `/v0/find` excludes `_wiki/` paths by default and — importantly — does NOT cover them even when the caller passes `prefix=_wiki/...`. Wiki pages are not embedded by the pipeline (they\'re system-generated output, not user input), so `embed_chunks` has no rows for them and the join returns empty. Use `wiki_search` (or `list`/`grep` with a `_wiki/` prefix) for the wiki layer.  **Embedding availability:** when `GEMINI_API_KEY` is not configured, `mode=semantic` returns 503; `mode=hybrid` logs a warning and falls back to lexical-only; `mode=lexical` is unaffected.
     * Hybrid passage retrieval over the full file body
     */
    async findV0FindGet(requestParameters: FindV0FindGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FindPage> {
        const response = await this.findV0FindGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getArtifactByIdMetaV0ArtifactsArtIdMetaGet without sending the request
     */
    async getArtifactByIdMetaV0ArtifactsArtIdMetaGetRequestOpts(requestParameters: GetArtifactByIdMetaV0ArtifactsArtIdMetaGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling getArtifactByIdMetaV0ArtifactsArtIdMetaGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/meta`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Artifact metadata by stable ID (same shape as path /meta)
     */
    async getArtifactByIdMetaV0ArtifactsArtIdMetaGetRaw(requestParameters: GetArtifactByIdMetaV0ArtifactsArtIdMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.getArtifactByIdMetaV0ArtifactsArtIdMetaGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Artifact metadata by stable ID (same shape as path /meta)
     */
    async getArtifactByIdMetaV0ArtifactsArtIdMetaGet(requestParameters: GetArtifactByIdMetaV0ArtifactsArtIdMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.getArtifactByIdMetaV0ArtifactsArtIdMetaGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getArtifactByIdV0ArtifactsArtIdGet without sending the request
     */
    async getArtifactByIdV0ArtifactsArtIdGetRequestOpts(requestParameters: GetArtifactByIdV0ArtifactsArtIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling getArtifactByIdV0ArtifactsArtIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Canonical lookup of an artifact by its stable ID
     */
    async getArtifactByIdV0ArtifactsArtIdGetRaw(requestParameters: GetArtifactByIdV0ArtifactsArtIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.getArtifactByIdV0ArtifactsArtIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Canonical lookup of an artifact by its stable ID
     */
    async getArtifactByIdV0ArtifactsArtIdGet(requestParameters: GetArtifactByIdV0ArtifactsArtIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.getArtifactByIdV0ArtifactsArtIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getArtifactMetaV0ArtifactsPathMetaGet without sending the request
     */
    async getArtifactMetaV0ArtifactsPathMetaGetRequestOpts(requestParameters: GetArtifactMetaV0ArtifactsPathMetaGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling getArtifactMetaV0ArtifactsPathMetaGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{path}/meta`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get Artifact Meta
     */
    async getArtifactMetaV0ArtifactsPathMetaGetRaw(requestParameters: GetArtifactMetaV0ArtifactsPathMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.getArtifactMetaV0ArtifactsPathMetaGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Get Artifact Meta
     */
    async getArtifactMetaV0ArtifactsPathMetaGet(requestParameters: GetArtifactMetaV0ArtifactsPathMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.getArtifactMetaV0ArtifactsPathMetaGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGet without sending the request
     */
    async getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequestOpts(requestParameters: GetArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGet().'
            );
        }

        if (requestParameters['versionNumber'] == null) {
            throw new runtime.RequiredError(
                'versionNumber',
                'Required parameter "versionNumber" was null or undefined when calling getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/versions/{version_number}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));
        urlPath = urlPath.replace('{version_number}', encodeURIComponent(String(requestParameters['versionNumber'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Metadata for a specific version of an artifact
     */
    async getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRaw(requestParameters: GetArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VersionOut>> {
        const requestOptions = await this.getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VersionOutFromJSON(jsonValue));
    }

    /**
     * Metadata for a specific version of an artifact
     */
    async getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGet(requestParameters: GetArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VersionOut> {
        const response = await this.getArtifactVersionV0ArtifactsArtIdVersionsVersionNumberGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getDriveRouteV0DrivesDriveIdGet without sending the request
     */
    async getDriveRouteV0DrivesDriveIdGetRequestOpts(requestParameters: GetDriveRouteV0DrivesDriveIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling getDriveRouteV0DrivesDriveIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Identical to `GET /v0/drives/me` — the by-id singleton so `Location`-style URLs and scripted clients can address the drive canonically. The path-param `drive_id` MUST match the authenticated drive (mirrors the delete/trash routes\' no-leak 404). Emits the drive\'s composite `ETag` header (`\"<drv_id>.0.<metageneration>\"`).
     * Drive overview by id (same shape as /drives/me)
     */
    async getDriveRouteV0DrivesDriveIdGetRaw(requestParameters: GetDriveRouteV0DrivesDriveIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveReadOut>> {
        const requestOptions = await this.getDriveRouteV0DrivesDriveIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveReadOutFromJSON(jsonValue));
    }

    /**
     * Identical to `GET /v0/drives/me` — the by-id singleton so `Location`-style URLs and scripted clients can address the drive canonically. The path-param `drive_id` MUST match the authenticated drive (mirrors the delete/trash routes\' no-leak 404). Emits the drive\'s composite `ETag` header (`\"<drv_id>.0.<metageneration>\"`).
     * Drive overview by id (same shape as /drives/me)
     */
    async getDriveRouteV0DrivesDriveIdGet(requestParameters: GetDriveRouteV0DrivesDriveIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveReadOut> {
        const response = await this.getDriveRouteV0DrivesDriveIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFeedbackStatusV0FeedbackFbkIdGet without sending the request
     */
    async getFeedbackStatusV0FeedbackFbkIdGetRequestOpts(requestParameters: GetFeedbackStatusV0FeedbackFbkIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fbkId'] == null) {
            throw new runtime.RequiredError(
                'fbkId',
                'Required parameter "fbkId" was null or undefined when calling getFeedbackStatusV0FeedbackFbkIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/feedback/{fbk_id}`;
        urlPath = urlPath.replace('{fbk_id}', encodeURIComponent(String(requestParameters['fbkId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Lifecycle status of feedback THIS drive filed. Foreign tickets read as 404 — indistinguishable from absent.
     * Get Feedback Status
     */
    async getFeedbackStatusV0FeedbackFbkIdGetRaw(requestParameters: GetFeedbackStatusV0FeedbackFbkIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FeedbackStatusOut>> {
        const requestOptions = await this.getFeedbackStatusV0FeedbackFbkIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FeedbackStatusOutFromJSON(jsonValue));
    }

    /**
     * Lifecycle status of feedback THIS drive filed. Foreign tickets read as 404 — indistinguishable from absent.
     * Get Feedback Status
     */
    async getFeedbackStatusV0FeedbackFbkIdGet(requestParameters: GetFeedbackStatusV0FeedbackFbkIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FeedbackStatusOut> {
        const response = await this.getFeedbackStatusV0FeedbackFbkIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFolderByIdMetaV0FoldersFldIdMetaGet without sending the request
     */
    async getFolderByIdMetaV0FoldersFldIdMetaGetRequestOpts(requestParameters: GetFolderByIdMetaV0FoldersFldIdMetaGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling getFolderByIdMetaV0FoldersFldIdMetaGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}/meta`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Folder metadata by stable ID (same shape as the bare id route)
     */
    async getFolderByIdMetaV0FoldersFldIdMetaGetRaw(requestParameters: GetFolderByIdMetaV0FoldersFldIdMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.getFolderByIdMetaV0FoldersFldIdMetaGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Folder metadata by stable ID (same shape as the bare id route)
     */
    async getFolderByIdMetaV0FoldersFldIdMetaGet(requestParameters: GetFolderByIdMetaV0FoldersFldIdMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.getFolderByIdMetaV0FoldersFldIdMetaGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFolderByIdV0FoldersFldIdGet without sending the request
     */
    async getFolderByIdV0FoldersFldIdGetRequestOpts(requestParameters: GetFolderByIdV0FoldersFldIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling getFolderByIdV0FoldersFldIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Canonical lookup of a folder by its stable ID
     */
    async getFolderByIdV0FoldersFldIdGetRaw(requestParameters: GetFolderByIdV0FoldersFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.getFolderByIdV0FoldersFldIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Canonical lookup of a folder by its stable ID
     */
    async getFolderByIdV0FoldersFldIdGet(requestParameters: GetFolderByIdV0FoldersFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.getFolderByIdV0FoldersFldIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFolderByPathMetaV0FoldersPathMetaGet without sending the request
     */
    async getFolderByPathMetaV0FoldersPathMetaGetRequestOpts(requestParameters: GetFolderByPathMetaV0FoldersPathMetaGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling getFolderByPathMetaV0FoldersPathMetaGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}/meta`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Folder metadata by path (same shape as the bare path route)
     */
    async getFolderByPathMetaV0FoldersPathMetaGetRaw(requestParameters: GetFolderByPathMetaV0FoldersPathMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.getFolderByPathMetaV0FoldersPathMetaGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Folder metadata by path (same shape as the bare path route)
     */
    async getFolderByPathMetaV0FoldersPathMetaGet(requestParameters: GetFolderByPathMetaV0FoldersPathMetaGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.getFolderByPathMetaV0FoldersPathMetaGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFolderByPathV0FoldersPathGet without sending the request
     */
    async getFolderByPathV0FoldersPathGetRequestOpts(requestParameters: GetFolderByPathV0FoldersPathGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling getFolderByPathV0FoldersPathGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Read folder metadata by path
     */
    async getFolderByPathV0FoldersPathGetRaw(requestParameters: GetFolderByPathV0FoldersPathGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.getFolderByPathV0FoldersPathGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Read folder metadata by path
     */
    async getFolderByPathV0FoldersPathGet(requestParameters: GetFolderByPathV0FoldersPathGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.getFolderByPathV0FoldersPathGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getGrantRouteV0GrantsGrnIdGet without sending the request
     */
    async getGrantRouteV0GrantsGrnIdGetRequestOpts(requestParameters: GetGrantRouteV0GrantsGrnIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['grnId'] == null) {
            throw new runtime.RequiredError(
                'grnId',
                'Required parameter "grnId" was null or undefined when calling getGrantRouteV0GrantsGrnIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/grants/{grn_id}`;
        urlPath = urlPath.replace('{grn_id}', encodeURIComponent(String(requestParameters['grnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The `Location` target of `POST /v0/grants`. Authorization mirrors DELETE: `can_manage` on the granted resource, or the caller IS the grant\'s own principal (a grantee may read — like revoke — their own grant). A revoked grant reads as 404 (same no-leak shape as a foreign/absent id); DELETE stays idempotent on it.
     * Read a single grant (can_manage, or the grant\'s own principal)
     */
    async getGrantRouteV0GrantsGrnIdGetRaw(requestParameters: GetGrantRouteV0GrantsGrnIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        const requestOptions = await this.getGrantRouteV0GrantsGrnIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * The `Location` target of `POST /v0/grants`. Authorization mirrors DELETE: `can_manage` on the granted resource, or the caller IS the grant\'s own principal (a grantee may read — like revoke — their own grant). A revoked grant reads as 404 (same no-leak shape as a foreign/absent id); DELETE stays idempotent on it.
     * Read a single grant (can_manage, or the grant\'s own principal)
     */
    async getGrantRouteV0GrantsGrnIdGet(requestParameters: GetGrantRouteV0GrantsGrnIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.getGrantRouteV0GrantsGrnIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getJobLogsV0JobsJobIdLogsGet without sending the request
     */
    async getJobLogsV0JobsJobIdLogsGetRequestOpts(requestParameters: GetJobLogsV0JobsJobIdLogsGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling getJobLogsV0JobsJobIdLogsGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/jobs/{job_id}/logs`;
        urlPath = urlPath.replace('{job_id}', encodeURIComponent(String(requestParameters['jobId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Raw compile log (text/plain)
     */
    async getJobLogsV0JobsJobIdLogsGetRaw(requestParameters: GetJobLogsV0JobsJobIdLogsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const requestOptions = await this.getJobLogsV0JobsJobIdLogsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Raw compile log (text/plain)
     */
    async getJobLogsV0JobsJobIdLogsGet(requestParameters: GetJobLogsV0JobsJobIdLogsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.getJobLogsV0JobsJobIdLogsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getJobV0JobsJobIdGet without sending the request
     */
    async getJobV0JobsJobIdGetRequestOpts(requestParameters: GetJobV0JobsJobIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling getJobV0JobsJobIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/jobs/{job_id}`;
        urlPath = urlPath.replace('{job_id}', encodeURIComponent(String(requestParameters['jobId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Poll a job
     */
    async getJobV0JobsJobIdGetRaw(requestParameters: GetJobV0JobsJobIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileJobOut>> {
        const requestOptions = await this.getJobV0JobsJobIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileJobOutFromJSON(jsonValue));
    }

    /**
     * Poll a job
     */
    async getJobV0JobsJobIdGet(requestParameters: GetJobV0JobsJobIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileJobOut> {
        const response = await this.getJobV0JobsJobIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getProjectV0ProjectsFldIdGet without sending the request
     */
    async getProjectV0ProjectsFldIdGetRequestOpts(requestParameters: GetProjectV0ProjectsFldIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling getProjectV0ProjectsFldIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/projects/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get a project\'s compile config
     */
    async getProjectV0ProjectsFldIdGetRaw(requestParameters: GetProjectV0ProjectsFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileProjectOut>> {
        const requestOptions = await this.getProjectV0ProjectsFldIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileProjectOutFromJSON(jsonValue));
    }

    /**
     * Get a project\'s compile config
     */
    async getProjectV0ProjectsFldIdGet(requestParameters: GetProjectV0ProjectsFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileProjectOut> {
        const response = await this.getProjectV0ProjectsFldIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getShareRouteV0SharesShrIdGet without sending the request
     */
    async getShareRouteV0SharesShrIdGetRequestOpts(requestParameters: GetShareRouteV0SharesShrIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shrId'] == null) {
            throw new runtime.RequiredError(
                'shrId',
                'Required parameter "shrId" was null or undefined when calling getShareRouteV0SharesShrIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/shares/{shr_id}`;
        urlPath = urlPath.replace('{shr_id}', encodeURIComponent(String(requestParameters['shrId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The `Location` target of `POST /v0/shares`. Metadata ONLY — `ShareOut` never carries the raw `share_key`/URL (returned exactly once at mint/rotate, §4.5). Authorization mirrors DELETE: `can_manage` on the shared resource. A revoked share reads as 404 (same no-leak shape as a foreign/absent id).
     * Read a single share link\'s metadata (requires can_manage)
     */
    async getShareRouteV0SharesShrIdGetRaw(requestParameters: GetShareRouteV0SharesShrIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareOut>> {
        const requestOptions = await this.getShareRouteV0SharesShrIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareOutFromJSON(jsonValue));
    }

    /**
     * The `Location` target of `POST /v0/shares`. Metadata ONLY — `ShareOut` never carries the raw `share_key`/URL (returned exactly once at mint/rotate, §4.5). Authorization mirrors DELETE: `can_manage` on the shared resource. A revoked share reads as 404 (same no-leak shape as a foreign/absent id).
     * Read a single share link\'s metadata (requires can_manage)
     */
    async getShareRouteV0SharesShrIdGet(requestParameters: GetShareRouteV0SharesShrIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareOut> {
        const response = await this.getShareRouteV0SharesShrIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getUploadStatusV0UploadsUploadIdGet without sending the request
     */
    async getUploadStatusV0UploadsUploadIdGetRequestOpts(requestParameters: GetUploadStatusV0UploadsUploadIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['uploadId'] == null) {
            throw new runtime.RequiredError(
                'uploadId',
                'Required parameter "uploadId" was null or undefined when calling getUploadStatusV0UploadsUploadIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/uploads/{upload_id}`;
        urlPath = urlPath.replace('{upload_id}', encodeURIComponent(String(requestParameters['uploadId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Report the live state of an upload session begun at `/v0/uploads`. `state` is derived: `initiated` (open — PUT the bytes then commit), `committed` (artifact created), `aborted` (released via DELETE), or `expired` (past `expires_at` without a commit). Read-only; charges the read budget.
     * Get the status of a large (direct-to-GCS) upload session
     */
    async getUploadStatusV0UploadsUploadIdGetRaw(requestParameters: GetUploadStatusV0UploadsUploadIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UploadStatusOut>> {
        const requestOptions = await this.getUploadStatusV0UploadsUploadIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UploadStatusOutFromJSON(jsonValue));
    }

    /**
     * Report the live state of an upload session begun at `/v0/uploads`. `state` is derived: `initiated` (open — PUT the bytes then commit), `committed` (artifact created), `aborted` (released via DELETE), or `expired` (past `expires_at` without a commit). Read-only; charges the read budget.
     * Get the status of a large (direct-to-GCS) upload session
     */
    async getUploadStatusV0UploadsUploadIdGet(requestParameters: GetUploadStatusV0UploadsUploadIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UploadStatusOut> {
        const response = await this.getUploadStatusV0UploadsUploadIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for healthHealthGet without sending the request
     */
    async healthHealthGetRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/health`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Liveness + DB-reachability probe. Used by Cloud Run / k8s healthchecks and any uptime monitor. Returns 200 only if the DB pool can serve a trivial query; 503 otherwise so the orchestrator can pull the instance out of rotation.  NOTE: route is `/health`, NOT `/healthz`. Google\'s edge infrastructure intercepts `/healthz` (legacy kubernetes-reserved path) and returns a generic 404 before traffic reaches Cloud Run — discovered the hard way during the first prod deploy. Don\'t rename back.
     * Health
     */
    async healthHealthGetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HealthOut>> {
        const requestOptions = await this.healthHealthGetRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HealthOutFromJSON(jsonValue));
    }

    /**
     * Liveness + DB-reachability probe. Used by Cloud Run / k8s healthchecks and any uptime monitor. Returns 200 only if the DB pool can serve a trivial query; 503 otherwise so the orchestrator can pull the instance out of rotation.  NOTE: route is `/health`, NOT `/healthz`. Google\'s edge infrastructure intercepts `/healthz` (legacy kubernetes-reserved path) and returns a generic 404 before traffic reaches Cloud Run — discovered the hard way during the first prod deploy. Don\'t rename back.
     * Health
     */
    async healthHealthGet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HealthOut> {
        const response = await this.healthHealthGetRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listArtifactVersionsV0ArtifactsArtIdVersionsGet without sending the request
     */
    async listArtifactVersionsV0ArtifactsArtIdVersionsGetRequestOpts(requestParameters: ListArtifactVersionsV0ArtifactsArtIdVersionsGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling listArtifactVersionsV0ArtifactsArtIdVersionsGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/versions`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns versions in descending `version_number` order. Cursor pagination via `?cursor=<token>`; `next_cursor` is non-null when the page is full and more older versions may exist.
     * List versions of an artifact, newest first
     */
    async listArtifactVersionsV0ArtifactsArtIdVersionsGetRaw(requestParameters: ListArtifactVersionsV0ArtifactsArtIdVersionsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VersionPage>> {
        const requestOptions = await this.listArtifactVersionsV0ArtifactsArtIdVersionsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VersionPageFromJSON(jsonValue));
    }

    /**
     * Returns versions in descending `version_number` order. Cursor pagination via `?cursor=<token>`; `next_cursor` is non-null when the page is full and more older versions may exist.
     * List versions of an artifact, newest first
     */
    async listArtifactVersionsV0ArtifactsArtIdVersionsGet(requestParameters: ListArtifactVersionsV0ArtifactsArtIdVersionsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VersionPage> {
        const response = await this.listArtifactVersionsV0ArtifactsArtIdVersionsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listArtifactsV0ArtifactsGet without sending the request
     */
    async listArtifactsV0ArtifactsGetRequestOpts(requestParameters: ListArtifactsV0ArtifactsGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['prefix'] != null) {
            queryParameters['prefix'] = requestParameters['prefix'];
        }

        if (requestParameters['label'] != null) {
            queryParameters['label'] = requestParameters['label'];
        }

        if (requestParameters['fileType'] != null) {
            queryParameters['file_type'] = requestParameters['fileType'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns artifacts sorted by path. Filter by `prefix`, `label` (repeatable + AND-combined), and `file_type`.  **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page. `next_cursor` is `null` on the final page. Filters MUST stay consistent across pages — the cursor encodes only the keyset position, not the filter set, so the client is responsible for re-sending the same filter on each page.
     * List artifacts in the drive
     */
    async listArtifactsV0ArtifactsGetRaw(requestParameters: ListArtifactsV0ArtifactsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Page>> {
        const requestOptions = await this.listArtifactsV0ArtifactsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageFromJSON(jsonValue));
    }

    /**
     * Returns artifacts sorted by path. Filter by `prefix`, `label` (repeatable + AND-combined), and `file_type`.  **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page. `next_cursor` is `null` on the final page. Filters MUST stay consistent across pages — the cursor encodes only the keyset position, not the filter set, so the client is responsible for re-sending the same filter on each page.
     * List artifacts in the drive
     */
    async listArtifactsV0ArtifactsGet(requestParameters: ListArtifactsV0ArtifactsGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Page> {
        const response = await this.listArtifactsV0ArtifactsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listEventsRouteV0EventsGet without sending the request
     */
    async listEventsRouteV0EventsGetRequestOpts(requestParameters: ListEventsRouteV0EventsGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['artId'] != null) {
            queryParameters['art_id'] = requestParameters['artId'];
        }

        if (requestParameters['action'] != null) {
            queryParameters['action'] = requestParameters['action'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = (requestParameters['since'] as any).toISOString();
        }

        if (requestParameters['before'] != null) {
            queryParameters['before'] = (requestParameters['before'] as any).toISOString();
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/events`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns events newest-first. Filters compose with AND.  **Cursor pagination:** pass the oldest event\'s `created_at` from the previous page as `before` to fetch the next page back in time. Combine `since` + `before` to bound a window.
     * Read the append-only event log for the authenticated drive
     */
    async listEventsRouteV0EventsGetRaw(requestParameters: ListEventsRouteV0EventsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EventPage>> {
        const requestOptions = await this.listEventsRouteV0EventsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EventPageFromJSON(jsonValue));
    }

    /**
     * Returns events newest-first. Filters compose with AND.  **Cursor pagination:** pass the oldest event\'s `created_at` from the previous page as `before` to fetch the next page back in time. Combine `since` + `before` to bound a window.
     * Read the append-only event log for the authenticated drive
     */
    async listEventsRouteV0EventsGet(requestParameters: ListEventsRouteV0EventsGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EventPage> {
        const response = await this.listEventsRouteV0EventsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listGrantsRouteV0GrantsGet without sending the request
     */
    async listGrantsRouteV0GrantsGetRequestOpts(requestParameters: ListGrantsRouteV0GrantsGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['resource'] == null) {
            throw new runtime.RequiredError(
                'resource',
                'Required parameter "resource" was null or undefined when calling listGrantsRouteV0GrantsGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['resource'] != null) {
            queryParameters['resource'] = requestParameters['resource'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/grants`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected. The `resource` filter must be re-sent on every page — the cursor encodes only the keyset position.
     * List live grants on a resource (requires can_manage)
     */
    async listGrantsRouteV0GrantsGetRaw(requestParameters: ListGrantsRouteV0GrantsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantList>> {
        const requestOptions = await this.listGrantsRouteV0GrantsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantListFromJSON(jsonValue));
    }

    /**
     * **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected. The `resource` filter must be re-sent on every page — the cursor encodes only the keyset position.
     * List live grants on a resource (requires can_manage)
     */
    async listGrantsRouteV0GrantsGet(requestParameters: ListGrantsRouteV0GrantsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantList> {
        const response = await this.listGrantsRouteV0GrantsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listProjectJobsV0ProjectsFldIdJobsGet without sending the request
     */
    async listProjectJobsV0ProjectsFldIdJobsGetRequestOpts(requestParameters: ListProjectJobsV0ProjectsFldIdJobsGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling listProjectJobsV0ProjectsFldIdJobsGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/projects/{fld_id}/jobs`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List compile jobs newest first in stable `(created_at, job_id)` descending order. Pass a non-null `next_cursor` back as `cursor` to continue; malformed cursors return `400 BAD_CURSOR`. The cursor contains only the keyset position, so a `status` filter must be re-sent unchanged on every page. `limit` retains its existing default of 50 and validated range of 1 through 200.
     * List a project\'s jobs
     */
    async listProjectJobsV0ProjectsFldIdJobsGetRaw(requestParameters: ListProjectJobsV0ProjectsFldIdJobsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileJobListOut>> {
        const requestOptions = await this.listProjectJobsV0ProjectsFldIdJobsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileJobListOutFromJSON(jsonValue));
    }

    /**
     * List compile jobs newest first in stable `(created_at, job_id)` descending order. Pass a non-null `next_cursor` back as `cursor` to continue; malformed cursors return `400 BAD_CURSOR`. The cursor contains only the keyset position, so a `status` filter must be re-sent unchanged on every page. `limit` retains its existing default of 50 and validated range of 1 through 200.
     * List a project\'s jobs
     */
    async listProjectJobsV0ProjectsFldIdJobsGet(requestParameters: ListProjectJobsV0ProjectsFldIdJobsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileJobListOut> {
        const response = await this.listProjectJobsV0ProjectsFldIdJobsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listSharesRouteV0SharesGet without sending the request
     */
    async listSharesRouteV0SharesGetRequestOpts(requestParameters: ListSharesRouteV0SharesGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['resource'] == null) {
            throw new runtime.RequiredError(
                'resource',
                'Required parameter "resource" was null or undefined when calling listSharesRouteV0SharesGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['resource'] != null) {
            queryParameters['resource'] = requestParameters['resource'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/shares`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected. The `resource` filter must be re-sent on every page — the cursor encodes only the keyset position.
     * List live share links on a resource (requires can_manage)
     */
    async listSharesRouteV0SharesGetRaw(requestParameters: ListSharesRouteV0SharesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareList>> {
        const requestOptions = await this.listSharesRouteV0SharesGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareListFromJSON(jsonValue));
    }

    /**
     * **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected. The `resource` filter must be re-sent on every page — the cursor encodes only the keyset position.
     * List live share links on a resource (requires can_manage)
     */
    async listSharesRouteV0SharesGet(requestParameters: ListSharesRouteV0SharesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareList> {
        const response = await this.listSharesRouteV0SharesGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTrashRouteV0DrivesDriveIdTrashGet without sending the request
     */
    async listTrashRouteV0DrivesDriveIdTrashGetRequestOpts(requestParameters: ListTrashRouteV0DrivesDriveIdTrashGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling listTrashRouteV0DrivesDriveIdTrashGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/trash`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns soft-deleted artifacts on the drive plus the drive\'s own soft-delete state (if applicable). The path-param `drive_id` MUST match the authenticated drive.  **Compatibility window:** `limit` or `cursor` opts into cursor pagination. Unadorned requests retain the legacy complete result during the migration window. Paginated requests are clamped to 1–100 items (default 50 when only `cursor` is supplied). `items` is canonical; `artifacts` is a deprecated same-value alias.
     * List the authenticated drive\'s trash
     */
    async listTrashRouteV0DrivesDriveIdTrashGetRaw(requestParameters: ListTrashRouteV0DrivesDriveIdTrashGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrashOut>> {
        const requestOptions = await this.listTrashRouteV0DrivesDriveIdTrashGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrashOutFromJSON(jsonValue));
    }

    /**
     * Returns soft-deleted artifacts on the drive plus the drive\'s own soft-delete state (if applicable). The path-param `drive_id` MUST match the authenticated drive.  **Compatibility window:** `limit` or `cursor` opts into cursor pagination. Unadorned requests retain the legacy complete result during the migration window. Paginated requests are clamped to 1–100 items (default 50 when only `cursor` is supplied). `items` is canonical; `artifacts` is a deprecated same-value alias.
     * List the authenticated drive\'s trash
     */
    async listTrashRouteV0DrivesDriveIdTrashGet(requestParameters: ListTrashRouteV0DrivesDriveIdTrashGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrashOut> {
        const response = await this.listTrashRouteV0DrivesDriveIdTrashGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for loginAuthLoginGet without sending the request
     */
    async loginAuthLoginGetRequestOpts(requestParameters: LoginAuthLoginGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['returnTo'] != null) {
            queryParameters['return_to'] = requestParameters['returnTo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/auth/login`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Begin a WorkOS sign-in flow.  Mints a pre-login state cookie (binds the OAuth flow to this browser — defense-in-depth against login-CSRF), signs a state payload, and redirects to AuthKit. The hosted AuthKit page lets the user pick Google OAuth, Microsoft OAuth, magic-link, password, or passkey; we don\'t care which — they all funnel back to /auth/callback with a `code` we exchange in D2.
     * Login
     */
    async loginAuthLoginGetRaw(requestParameters: LoginAuthLoginGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.loginAuthLoginGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Begin a WorkOS sign-in flow.  Mints a pre-login state cookie (binds the OAuth flow to this browser — defense-in-depth against login-CSRF), signs a state payload, and redirects to AuthKit. The hosted AuthKit page lets the user pick Google OAuth, Microsoft OAuth, magic-link, password, or passkey; we don\'t care which — they all funnel back to /auth/callback with a `code` we exchange in D2.
     * Login
     */
    async loginAuthLoginGet(requestParameters: LoginAuthLoginGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.loginAuthLoginGetRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for logoutAuthLogoutPost without sending the request
     */
    async logoutAuthLogoutPostRequestOpts(requestParameters: LogoutAuthLogoutPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['csrf'] == null) {
            throw new runtime.RequiredError(
                'csrf',
                'Required parameter "csrf" was null or undefined when calling logoutAuthLogoutPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['csrf'] != null) {
            formParams.append('csrf', requestParameters['csrf'] as any);
        }


        let urlPath = `/auth/logout`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Terminate both the local session AND the upstream WorkOS session.  Without the WorkOS-side termination, the next `/auth/login` flow silently re-authenticates the user through AuthKit\'s still-valid session cookie on `api.workos.com` — \"Sign out\" feels broken and a shared-browser user can\'t switch accounts. The recommended pattern (per https://workos.com/docs/authkit/sessions) is to redirect to the WorkOS logout endpoint with the `sid` we stashed during the callback; WorkOS clears its own session and returns the browser to our `return_to`.  Failure modes handled:   * No `workos_session_id` in the session (legacy v2 cookie issued     before this slice landed): fall back to local-only logout. The     upstream session lingers but the user\'s local state is cleared     — same UX as before this slice; cookie rotation on next sign-in     eventually overwrites it.   * SDK raises during `get_logout_url`: pure string formatting at     WorkOS\'s end, so the only realistic failure is a misconfigured     WorkOS dashboard (no Sign-out redirect registered). We catch     and fall back to local-only logout rather than 500ing — the     user clicked \"Sign out\", they should land somewhere, not on an     error page.
     * Logout
     */
    async logoutAuthLogoutPostRaw(requestParameters: LogoutAuthLogoutPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.logoutAuthLogoutPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Terminate both the local session AND the upstream WorkOS session.  Without the WorkOS-side termination, the next `/auth/login` flow silently re-authenticates the user through AuthKit\'s still-valid session cookie on `api.workos.com` — \"Sign out\" feels broken and a shared-browser user can\'t switch accounts. The recommended pattern (per https://workos.com/docs/authkit/sessions) is to redirect to the WorkOS logout endpoint with the `sid` we stashed during the callback; WorkOS clears its own session and returns the browser to our `return_to`.  Failure modes handled:   * No `workos_session_id` in the session (legacy v2 cookie issued     before this slice landed): fall back to local-only logout. The     upstream session lingers but the user\'s local state is cleared     — same UX as before this slice; cookie rotation on next sign-in     eventually overwrites it.   * SDK raises during `get_logout_url`: pure string formatting at     WorkOS\'s end, so the only realistic failure is a misconfigured     WorkOS dashboard (no Sign-out redirect registered). We catch     and fall back to local-only logout rather than 500ing — the     user clicked \"Sign out\", they should land somewhere, not on an     error page.
     * Logout
     */
    async logoutAuthLogoutPost(requestParameters: LogoutAuthLogoutPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.logoutAuthLogoutPostRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for meUsageV0DrivesMeUsageGet without sending the request
     */
    async meUsageV0DrivesMeUsageGetRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/me/usage`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Unified view of every metered dimension: storage (snapshot), writes (current hour), indexing ops + retrieval queries (current calendar month UTC). Each row carries `used` and `limit`; `limit: 0` means unlimited (the v0 free-tier default for the two monthly counters). Reads are de-throttled — there is no hourly read budget; the monthly read count appears under `ops_this_month.reads`.
     * Current-period usage + caps for the authenticated drive
     */
    async meUsageV0DrivesMeUsageGetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveUsageOut>> {
        const requestOptions = await this.meUsageV0DrivesMeUsageGetRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveUsageOutFromJSON(jsonValue));
    }

    /**
     * Unified view of every metered dimension: storage (snapshot), writes (current hour), indexing ops + retrieval queries (current calendar month UTC). Each row carries `used` and `limit`; `limit: 0` means unlimited (the v0 free-tier default for the two monthly counters). Reads are de-throttled — there is no hourly read budget; the monthly read count appears under `ops_this_month.reads`.
     * Current-period usage + caps for the authenticated drive
     */
    async meUsageV0DrivesMeUsageGet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveUsageOut> {
        const response = await this.meUsageV0DrivesMeUsageGetRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for meV0DrivesMeGet without sending the request
     */
    async meV0DrivesMeGetRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/me`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Drive overview for the authenticated bearer token.  Wire-protocol preservation (WorkOS integration §6): the `email` field is preserved in the response shape; its meaning is now \"the drive\'s owner\'s email\" (via `drives.owner_user_id` → `users.email`, joined in `auth.resolve_drive`). For solo signups this equals v0 behavior — the email the user signed up with. Returns null if the owner has been hard-purged. `organization_id` is a new additive field, as are `metageneration` / `etag` (also emitted as the `ETag` header).
     * Me
     */
    async meV0DrivesMeGetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveReadOut>> {
        const requestOptions = await this.meV0DrivesMeGetRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveReadOutFromJSON(jsonValue));
    }

    /**
     * Drive overview for the authenticated bearer token.  Wire-protocol preservation (WorkOS integration §6): the `email` field is preserved in the response shape; its meaning is now \"the drive\'s owner\'s email\" (via `drives.owner_user_id` → `users.email`, joined in `auth.resolve_drive`). For solo signups this equals v0 behavior — the email the user signed up with. Returns null if the owner has been hard-purged. `organization_id` is a new additive field, as are `metageneration` / `etag` (also emitted as the `ETag` header).
     * Me
     */
    async meV0DrivesMeGet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveReadOut> {
        const response = await this.meV0DrivesMeGetRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for moveArtifactRouteV0ArtifactsArtIdMovePost without sending the request
     */
    async moveArtifactRouteV0ArtifactsArtIdMovePostRequestOpts(requestParameters: MoveArtifactRouteV0ArtifactsArtIdMovePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling moveArtifactRouteV0ArtifactsArtIdMovePost().'
            );
        }

        if (requestParameters['artifactMoveIn'] == null) {
            throw new runtime.RequiredError(
                'artifactMoveIn',
                'Required parameter "artifactMoveIn" was null or undefined when calling moveArtifactRouteV0ArtifactsArtIdMovePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/move`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ArtifactMoveInToJSON(requestParameters['artifactMoveIn']),
        };
    }

    /**
     * Canonical artifact move/rename, keyed by the stable `art_…` ID (the artifact analogue of `POST /v0/folders/{fld_id}/move`). Moves the artifact to a new `path` on the same drive; ID, version history, source refs, labels, metadata, and the underlying CAS blob are all preserved — only `path` and `updated_at` change, and the move does NOT bump `version_number`.  The row UPDATE and the emitted `artifact.renamed` event commit in a SINGLE transaction — a failure leaves the artifact fully unchanged.  Returns 409 PATH_CONFLICT if the target `path` is already taken; 404 ARTIFACT_NOT_FOUND for an unknown id; 403 WIKI_RESERVED for a `_wiki/` / `_compiled/` target. Honors `If-Match` (→ 412 PRECONDITION_FAILED). Use `X-AgentDrive-Actor` to attach attribution to the emitted `artifact.renamed` event.
     * Rename / move an artifact to a new path
     */
    async moveArtifactRouteV0ArtifactsArtIdMovePostRaw(requestParameters: MoveArtifactRouteV0ArtifactsArtIdMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.moveArtifactRouteV0ArtifactsArtIdMovePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Canonical artifact move/rename, keyed by the stable `art_…` ID (the artifact analogue of `POST /v0/folders/{fld_id}/move`). Moves the artifact to a new `path` on the same drive; ID, version history, source refs, labels, metadata, and the underlying CAS blob are all preserved — only `path` and `updated_at` change, and the move does NOT bump `version_number`.  The row UPDATE and the emitted `artifact.renamed` event commit in a SINGLE transaction — a failure leaves the artifact fully unchanged.  Returns 409 PATH_CONFLICT if the target `path` is already taken; 404 ARTIFACT_NOT_FOUND for an unknown id; 403 WIKI_RESERVED for a `_wiki/` / `_compiled/` target. Honors `If-Match` (→ 412 PRECONDITION_FAILED). Use `X-AgentDrive-Actor` to attach attribution to the emitted `artifact.renamed` event.
     * Rename / move an artifact to a new path
     */
    async moveArtifactRouteV0ArtifactsArtIdMovePost(requestParameters: MoveArtifactRouteV0ArtifactsArtIdMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.moveArtifactRouteV0ArtifactsArtIdMovePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for moveFolderByIdV0FoldersFldIdMovePost without sending the request
     */
    async moveFolderByIdV0FoldersFldIdMovePostRequestOpts(requestParameters: MoveFolderByIdV0FoldersFldIdMovePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling moveFolderByIdV0FoldersFldIdMovePost().'
            );
        }

        if (requestParameters['folderMoveIn'] == null) {
            throw new runtime.RequiredError(
                'folderMoveIn',
                'Required parameter "folderMoveIn" was null or undefined when calling moveFolderByIdV0FoldersFldIdMovePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}/move`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FolderMoveInToJSON(requestParameters['folderMoveIn']),
        };
    }

    /**
     * Rename / move a folder by stable ID (cascade descendants)
     */
    async moveFolderByIdV0FoldersFldIdMovePostRaw(requestParameters: MoveFolderByIdV0FoldersFldIdMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.moveFolderByIdV0FoldersFldIdMovePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Rename / move a folder by stable ID (cascade descendants)
     */
    async moveFolderByIdV0FoldersFldIdMovePost(requestParameters: MoveFolderByIdV0FoldersFldIdMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.moveFolderByIdV0FoldersFldIdMovePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for moveFolderByPathV0FoldersPathMovePost without sending the request
     */
    async moveFolderByPathV0FoldersPathMovePostRequestOpts(requestParameters: MoveFolderByPathV0FoldersPathMovePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling moveFolderByPathV0FoldersPathMovePost().'
            );
        }

        if (requestParameters['folderMoveIn'] == null) {
            throw new runtime.RequiredError(
                'folderMoveIn',
                'Required parameter "folderMoveIn" was null or undefined when calling moveFolderByPathV0FoldersPathMovePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}/move`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FolderMoveInToJSON(requestParameters['folderMoveIn']),
        };
    }

    /**
     * Move the folder identified by URL path to the body\'s `path`. All descendant folders + artifacts are path-prefix-updated in the same transaction. The folder\'s `fld_*` ID stays stable.  Returns 409 `FOLDER_PATH_CONFLICT` if the destination prefix collides with a live folder or artifact path.
     * Rename / move a folder (cascade-update descendants)
     */
    async moveFolderByPathV0FoldersPathMovePostRaw(requestParameters: MoveFolderByPathV0FoldersPathMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.moveFolderByPathV0FoldersPathMovePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Move the folder identified by URL path to the body\'s `path`. All descendant folders + artifacts are path-prefix-updated in the same transaction. The folder\'s `fld_*` ID stays stable.  Returns 409 `FOLDER_PATH_CONFLICT` if the destination prefix collides with a live folder or artifact path.
     * Rename / move a folder (cascade-update descendants)
     */
    async moveFolderByPathV0FoldersPathMovePost(requestParameters: MoveFolderByPathV0FoldersPathMovePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.moveFolderByPathV0FoldersPathMovePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for patchArtifactRouteV0ArtifactsArtIdPatch without sending the request
     */
    async patchArtifactRouteV0ArtifactsArtIdPatchRequestOpts(requestParameters: PatchArtifactRouteV0ArtifactsArtIdPatchRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling patchArtifactRouteV0ArtifactsArtIdPatch().'
            );
        }

        if (requestParameters['artifactPatchIn'] == null) {
            throw new runtime.RequiredError(
                'artifactPatchIn',
                'Required parameter "artifactPatchIn" was null or undefined when calling patchArtifactRouteV0ArtifactsArtIdPatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: ArtifactPatchInToJSON(requestParameters['artifactPatchIn']),
        };
    }

    /**
     * Metadata-only JSON-merge-patch update of a single artifact, keyed by its stable `art_…` ID. Every field in the body is optional; a field that is **omitted** is left unchanged, a field that is **present** is applied — with an explicit `null` / `[]` / `{}` meaning \"clear\". This mirrors the MCP `set_metadata` tool.  Editable fields:   * `labels` — replace the label set (`[]`/`null` clears).   * `metadata` — replace the free-form metadata object (`{}`/`null` clears).   * `source` — replace provenance refs (`null` clears).  **To move/rename an artifact, use `POST /v0/artifacts/{art_id}/move`** — PATCH no longer accepts `path`. The body is `extra=\"forbid\"`, so a stray field (notably a legacy `path`) is rejected with 422 rather than silently ignored.  Metadata edits do NOT create a new content version (no `version_number` / generation bump, no `artifact_versions` row) but DO bump the artifact\'s `metageneration` and `updated_at`.  Returns 400 BAD_LABELS / BAD_SOURCE for invalid metadata; 404 ARTIFACT_NOT_FOUND for an unknown id. Honors `If-Match`, which takes the composite ETag `\"<art_id>.<generation>.<metageneration>\"` and is compared as a whole tuple: ANY concurrent content **or** metadata change (a bumped generation OR metageneration) → 412 PRECONDITION_FAILED. There is no last-writer-wins gap for metadata-only edits. Use `X-AgentDrive-Actor` to attach attribution to the emitted `artifact.metadata_updated` event.
     * Edit artifact metadata (labels / metadata / source)
     */
    async patchArtifactRouteV0ArtifactsArtIdPatchRaw(requestParameters: PatchArtifactRouteV0ArtifactsArtIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.patchArtifactRouteV0ArtifactsArtIdPatchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Metadata-only JSON-merge-patch update of a single artifact, keyed by its stable `art_…` ID. Every field in the body is optional; a field that is **omitted** is left unchanged, a field that is **present** is applied — with an explicit `null` / `[]` / `{}` meaning \"clear\". This mirrors the MCP `set_metadata` tool.  Editable fields:   * `labels` — replace the label set (`[]`/`null` clears).   * `metadata` — replace the free-form metadata object (`{}`/`null` clears).   * `source` — replace provenance refs (`null` clears).  **To move/rename an artifact, use `POST /v0/artifacts/{art_id}/move`** — PATCH no longer accepts `path`. The body is `extra=\"forbid\"`, so a stray field (notably a legacy `path`) is rejected with 422 rather than silently ignored.  Metadata edits do NOT create a new content version (no `version_number` / generation bump, no `artifact_versions` row) but DO bump the artifact\'s `metageneration` and `updated_at`.  Returns 400 BAD_LABELS / BAD_SOURCE for invalid metadata; 404 ARTIFACT_NOT_FOUND for an unknown id. Honors `If-Match`, which takes the composite ETag `\"<art_id>.<generation>.<metageneration>\"` and is compared as a whole tuple: ANY concurrent content **or** metadata change (a bumped generation OR metageneration) → 412 PRECONDITION_FAILED. There is no last-writer-wins gap for metadata-only edits. Use `X-AgentDrive-Actor` to attach attribution to the emitted `artifact.metadata_updated` event.
     * Edit artifact metadata (labels / metadata / source)
     */
    async patchArtifactRouteV0ArtifactsArtIdPatch(requestParameters: PatchArtifactRouteV0ArtifactsArtIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.patchArtifactRouteV0ArtifactsArtIdPatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for patchFolderByIdV0FoldersFldIdPatch without sending the request
     */
    async patchFolderByIdV0FoldersFldIdPatchRequestOpts(requestParameters: PatchFolderByIdV0FoldersFldIdPatchRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling patchFolderByIdV0FoldersFldIdPatch().'
            );
        }

        if (requestParameters['folderPatchIn'] == null) {
            throw new runtime.RequiredError(
                'folderPatchIn',
                'Required parameter "folderPatchIn" was null or undefined when calling patchFolderByIdV0FoldersFldIdPatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: FolderPatchInToJSON(requestParameters['folderPatchIn']),
        };
    }

    /**
     * Update folder metadata by stable ID
     */
    async patchFolderByIdV0FoldersFldIdPatchRaw(requestParameters: PatchFolderByIdV0FoldersFldIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.patchFolderByIdV0FoldersFldIdPatchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Update folder metadata by stable ID
     */
    async patchFolderByIdV0FoldersFldIdPatch(requestParameters: PatchFolderByIdV0FoldersFldIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.patchFolderByIdV0FoldersFldIdPatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for patchFolderByPathV0FoldersPathPatch without sending the request
     */
    async patchFolderByPathV0FoldersPathPatchRequestOpts(requestParameters: PatchFolderByPathV0FoldersPathPatchRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling patchFolderByPathV0FoldersPathPatch().'
            );
        }

        if (requestParameters['folderPatchIn'] == null) {
            throw new runtime.RequiredError(
                'folderPatchIn',
                'Required parameter "folderPatchIn" was null or undefined when calling patchFolderByPathV0FoldersPathPatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: FolderPatchInToJSON(requestParameters['folderPatchIn']),
        };
    }

    /**
     * Partial update — field absence leaves the value unchanged; explicit `null` clears the field. Use the by-id endpoint (slice 2) when you need stable addressing across renames.
     * Update folder metadata by path
     */
    async patchFolderByPathV0FoldersPathPatchRaw(requestParameters: PatchFolderByPathV0FoldersPathPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderOut>> {
        const requestOptions = await this.patchFolderByPathV0FoldersPathPatchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderOutFromJSON(jsonValue));
    }

    /**
     * Partial update — field absence leaves the value unchanged; explicit `null` clears the field. Use the by-id endpoint (slice 2) when you need stable addressing across renames.
     * Update folder metadata by path
     */
    async patchFolderByPathV0FoldersPathPatch(requestParameters: PatchFolderByPathV0FoldersPathPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderOut> {
        const response = await this.patchFolderByPathV0FoldersPathPatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for patchGrantRouteV0GrantsGrnIdPatch without sending the request
     */
    async patchGrantRouteV0GrantsGrnIdPatchRequestOpts(requestParameters: PatchGrantRouteV0GrantsGrnIdPatchRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['grnId'] == null) {
            throw new runtime.RequiredError(
                'grnId',
                'Required parameter "grnId" was null or undefined when calling patchGrantRouteV0GrantsGrnIdPatch().'
            );
        }

        if (requestParameters['grantPatchIn'] == null) {
            throw new runtime.RequiredError(
                'grantPatchIn',
                'Required parameter "grantPatchIn" was null or undefined when calling patchGrantRouteV0GrantsGrnIdPatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/grants/{grn_id}`;
        urlPath = urlPath.replace('{grn_id}', encodeURIComponent(String(requestParameters['grnId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: GrantPatchInToJSON(requestParameters['grantPatchIn']),
        };
    }

    /**
     * Update a grant\'s role and/or expiry (requires can_manage)
     */
    async patchGrantRouteV0GrantsGrnIdPatchRaw(requestParameters: PatchGrantRouteV0GrantsGrnIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GrantOut>> {
        const requestOptions = await this.patchGrantRouteV0GrantsGrnIdPatchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GrantOutFromJSON(jsonValue));
    }

    /**
     * Update a grant\'s role and/or expiry (requires can_manage)
     */
    async patchGrantRouteV0GrantsGrnIdPatch(requestParameters: PatchGrantRouteV0GrantsGrnIdPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GrantOut> {
        const response = await this.patchGrantRouteV0GrantsGrnIdPatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for postDescribeV0QueryDescribePost without sending the request
     */
    async postDescribeV0QueryDescribePostRequestOpts(requestParameters: PostDescribeV0QueryDescribePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['describeIn'] == null) {
            throw new runtime.RequiredError(
                'describeIn',
                'Required parameter "describeIn" was null or undefined when calling postDescribeV0QueryDescribePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/query/describe`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DescribeInToJSON(requestParameters['describeIn']),
        };
    }

    /**
     * Describe a dataset\'s column schema
     */
    async postDescribeV0QueryDescribePostRaw(requestParameters: PostDescribeV0QueryDescribePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetDescriptionOut>> {
        const requestOptions = await this.postDescribeV0QueryDescribePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetDescriptionOutFromJSON(jsonValue));
    }

    /**
     * Describe a dataset\'s column schema
     */
    async postDescribeV0QueryDescribePost(requestParameters: PostDescribeV0QueryDescribePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetDescriptionOut> {
        const response = await this.postDescribeV0QueryDescribePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for postFeedbackV0FeedbackPost without sending the request
     */
    async postFeedbackV0FeedbackPostRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/feedback`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * File feedback. Body: `{kind, title, body, contact?, attachments?: [art_id, ...]}` — attachments are snapshotted from this drive\'s artifacts at submit time.
     * Post Feedback
     */
    async postFeedbackV0FeedbackPostRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FeedbackCreateOut>> {
        const requestOptions = await this.postFeedbackV0FeedbackPostRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FeedbackCreateOutFromJSON(jsonValue));
    }

    /**
     * File feedback. Body: `{kind, title, body, contact?, attachments?: [art_id, ...]}` — attachments are snapshotted from this drive\'s artifacts at submit time.
     * Post Feedback
     */
    async postFeedbackV0FeedbackPost(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FeedbackCreateOut> {
        const response = await this.postFeedbackV0FeedbackPostRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for postLookupValuesV0QueryLookupValuesPost without sending the request
     */
    async postLookupValuesV0QueryLookupValuesPostRequestOpts(requestParameters: PostLookupValuesV0QueryLookupValuesPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['lookupValuesIn'] == null) {
            throw new runtime.RequiredError(
                'lookupValuesIn',
                'Required parameter "lookupValuesIn" was null or undefined when calling postLookupValuesV0QueryLookupValuesPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/query/lookup-values`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LookupValuesInToJSON(requestParameters['lookupValuesIn']),
        };
    }

    /**
     * List distinct values of a dataset column
     */
    async postLookupValuesV0QueryLookupValuesPostRaw(requestParameters: PostLookupValuesV0QueryLookupValuesPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LookupValuesOut>> {
        const requestOptions = await this.postLookupValuesV0QueryLookupValuesPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LookupValuesOutFromJSON(jsonValue));
    }

    /**
     * List distinct values of a dataset column
     */
    async postLookupValuesV0QueryLookupValuesPost(requestParameters: PostLookupValuesV0QueryLookupValuesPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LookupValuesOut> {
        const response = await this.postLookupValuesV0QueryLookupValuesPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for postQueryV0QueryPost without sending the request
     */
    async postQueryV0QueryPostRequestOpts(requestParameters: PostQueryV0QueryPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['queryIn'] == null) {
            throw new runtime.RequiredError(
                'queryIn',
                'Required parameter "queryIn" was null or undefined when calling postQueryV0QueryPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/query`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QueryInToJSON(requestParameters['queryIn']),
        };
    }

    /**
     * Run a read-only SQL query over authorized datasets
     */
    async postQueryV0QueryPostRaw(requestParameters: PostQueryV0QueryPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResponsePostQueryV0QueryPost>> {
        const requestOptions = await this.postQueryV0QueryPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResponsePostQueryV0QueryPostFromJSON(jsonValue));
    }

    /**
     * Run a read-only SQL query over authorized datasets
     */
    async postQueryV0QueryPost(requestParameters: PostQueryV0QueryPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResponsePostQueryV0QueryPost> {
        const response = await this.postQueryV0QueryPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for putArtifactV0ArtifactsPathPut without sending the request
     */
    async putArtifactV0ArtifactsPathPutRequestOpts(requestParameters: PutArtifactV0ArtifactsPathPutRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling putArtifactV0ArtifactsPathPut().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['contentType'] != null) {
            headerParameters['content-type'] = String(requestParameters['contentType']);
        }

        if (requestParameters['xAgentdriveLabels'] != null) {
            headerParameters['x-agentdrive-labels'] = String(requestParameters['xAgentdriveLabels']);
        }

        if (requestParameters['xAgentdriveMetadata'] != null) {
            headerParameters['x-agentdrive-metadata'] = String(requestParameters['xAgentdriveMetadata']);
        }

        if (requestParameters['xAgentdriveSource'] != null) {
            headerParameters['x-agentdrive-source'] = String(requestParameters['xAgentdriveSource']);
        }

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['xAgentdriveChangeSummary'] != null) {
            headerParameters['x-agentdrive-change-summary'] = String(requestParameters['xAgentdriveChangeSummary']);
        }

        if (requestParameters['xAgentdriveChecksum'] != null) {
            headerParameters['x-agentdrive-checksum'] = String(requestParameters['xAgentdriveChecksum']);
        }

        if (requestParameters['contentMd5'] != null) {
            headerParameters['content-md5'] = String(requestParameters['contentMd5']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['if-none-match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{path}`;
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Upload an artifact at the given path. The path is treated as the artifact\'s location in the drive — re-uploading the same path overwrites in place (idempotent). Returns 201 when the artifact is created (no prior live artifact at the path), 200 on overwrite — mirroring `PUT /v0/folders/{path}`.  **Limits:** request body must not exceed **50 MB**. Path must be non-empty, ≤256 chars, only `[A-Za-z0-9_./-]`, no `..` segments, no leading/trailing slash. Per-token write rate limit: 100/hour.  **Optional headers.** Each preserves the existing artifact\'s value when omitted on an overwrite, and takes the create-default on a new path; send the header to replace it: - `X-AgentDrive-Labels`: comma-separated labels (e.g. `draft,report`); an empty value clears them. Each: lowercase `[a-z0-9_-]+`, ≤64 chars; ≤16 labels per artifact. - `X-AgentDrive-Metadata`: JSON object of agent-attached fields. - `X-AgentDrive-Source`: JSON `{\"refs\": [...]}` source provenance (present, including `{\"refs\": []}`, replaces). - `X-AgentDrive-Actor`: caller-supplied actor name (≤64 chars) for event-log attribution. Untrusted; never used for authz.  **Preconditions.** `If-Match: \"<id>.<gen>.<metagen>\"` makes the write conditional on the current composite ETag (→ 412 PRECONDITION_FAILED). `If-None-Match: *` is create-only: it succeeds only if no live artifact occupies the path (→ 412 CREATE_CONFLICT if one does). The two are mutually exclusive (→ 400 BAD_PRECONDITION).  **Integrity (optional).** `X-AgentDrive-Checksum: <algo>:<value>` (`sha256:<hex>` or `crc32c:<base64>`) or the standard `Content-MD5` (base64 MD5) is verified against the received bytes before they land (→ 400 CHECKSUM_MISMATCH on mismatch); no artifact is created on failure.
     * Upload (or overwrite) an artifact
     */
    async putArtifactV0ArtifactsPathPutRaw(requestParameters: PutArtifactV0ArtifactsPathPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.putArtifactV0ArtifactsPathPutRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Upload an artifact at the given path. The path is treated as the artifact\'s location in the drive — re-uploading the same path overwrites in place (idempotent). Returns 201 when the artifact is created (no prior live artifact at the path), 200 on overwrite — mirroring `PUT /v0/folders/{path}`.  **Limits:** request body must not exceed **50 MB**. Path must be non-empty, ≤256 chars, only `[A-Za-z0-9_./-]`, no `..` segments, no leading/trailing slash. Per-token write rate limit: 100/hour.  **Optional headers.** Each preserves the existing artifact\'s value when omitted on an overwrite, and takes the create-default on a new path; send the header to replace it: - `X-AgentDrive-Labels`: comma-separated labels (e.g. `draft,report`); an empty value clears them. Each: lowercase `[a-z0-9_-]+`, ≤64 chars; ≤16 labels per artifact. - `X-AgentDrive-Metadata`: JSON object of agent-attached fields. - `X-AgentDrive-Source`: JSON `{\"refs\": [...]}` source provenance (present, including `{\"refs\": []}`, replaces). - `X-AgentDrive-Actor`: caller-supplied actor name (≤64 chars) for event-log attribution. Untrusted; never used for authz.  **Preconditions.** `If-Match: \"<id>.<gen>.<metagen>\"` makes the write conditional on the current composite ETag (→ 412 PRECONDITION_FAILED). `If-None-Match: *` is create-only: it succeeds only if no live artifact occupies the path (→ 412 CREATE_CONFLICT if one does). The two are mutually exclusive (→ 400 BAD_PRECONDITION).  **Integrity (optional).** `X-AgentDrive-Checksum: <algo>:<value>` (`sha256:<hex>` or `crc32c:<base64>`) or the standard `Content-MD5` (base64 MD5) is verified against the received bytes before they land (→ 400 CHECKSUM_MISMATCH on mismatch); no artifact is created on failure.
     * Upload (or overwrite) an artifact
     */
    async putArtifactV0ArtifactsPathPut(requestParameters: PutArtifactV0ArtifactsPathPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.putArtifactV0ArtifactsPathPutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for putProjectV0ProjectsFldIdPut without sending the request
     */
    async putProjectV0ProjectsFldIdPutRequestOpts(requestParameters: PutProjectV0ProjectsFldIdPutRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling putProjectV0ProjectsFldIdPut().'
            );
        }

        if (requestParameters['projectConfigIn'] == null) {
            throw new runtime.RequiredError(
                'projectConfigIn',
                'Required parameter "projectConfigIn" was null or undefined when calling putProjectV0ProjectsFldIdPut().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/projects/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ProjectConfigInToJSON(requestParameters['projectConfigIn']),
        };
    }

    /**
     * Set a project\'s compile config (entrypoint/engine/auto_compile)
     */
    async putProjectV0ProjectsFldIdPutRaw(requestParameters: PutProjectV0ProjectsFldIdPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompileProjectOut>> {
        const requestOptions = await this.putProjectV0ProjectsFldIdPutRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompileProjectOutFromJSON(jsonValue));
    }

    /**
     * Set a project\'s compile config (entrypoint/engine/auto_compile)
     */
    async putProjectV0ProjectsFldIdPut(requestParameters: PutProjectV0ProjectsFldIdPutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompileProjectOut> {
        const response = await this.putProjectV0ProjectsFldIdPutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for redeemShareSShareKeyGet without sending the request
     */
    async redeemShareSShareKeyGetRequestOpts(requestParameters: RedeemShareSShareKeyGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shareKey'] == null) {
            throw new runtime.RequiredError(
                'shareKey',
                'Required parameter "shareKey" was null or undefined when calling redeemShareSShareKeyGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/s/{share_key}`;
        urlPath = urlPath.replace('{share_key}', encodeURIComponent(String(requestParameters['shareKey'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Redeem Share
     */
    async redeemShareSShareKeyGetRaw(requestParameters: RedeemShareSShareKeyGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareRedeemOut>> {
        const requestOptions = await this.redeemShareSShareKeyGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareRedeemOutFromJSON(jsonValue));
    }

    /**
     * Redeem Share
     */
    async redeemShareSShareKeyGet(requestParameters: RedeemShareSShareKeyGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareRedeemOut> {
        const response = await this.redeemShareSShareKeyGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for redeemShareWithPasswordSShareKeyPost without sending the request
     */
    async redeemShareWithPasswordSShareKeyPostRequestOpts(requestParameters: RedeemShareWithPasswordSShareKeyPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shareKey'] == null) {
            throw new runtime.RequiredError(
                'shareKey',
                'Required parameter "shareKey" was null or undefined when calling redeemShareWithPasswordSShareKeyPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['password'] != null) {
            formParams.append('password', requestParameters['password'] as any);
        }


        let urlPath = `/s/{share_key}`;
        urlPath = urlPath.replace('{share_key}', encodeURIComponent(String(requestParameters['shareKey'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Redeem Share With Password
     */
    async redeemShareWithPasswordSShareKeyPostRaw(requestParameters: RedeemShareWithPasswordSShareKeyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareRedeemOut>> {
        const requestOptions = await this.redeemShareWithPasswordSShareKeyPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareRedeemOutFromJSON(jsonValue));
    }

    /**
     * Redeem Share With Password
     */
    async redeemShareWithPasswordSShareKeyPost(requestParameters: RedeemShareWithPasswordSShareKeyPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareRedeemOut> {
        const response = await this.redeemShareWithPasswordSShareKeyPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for restoreArtifactV0ArtifactsArtIdRestorePost without sending the request
     */
    async restoreArtifactV0ArtifactsArtIdRestorePostRequestOpts(requestParameters: RestoreArtifactV0ArtifactsArtIdRestorePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling restoreArtifactV0ArtifactsArtIdRestorePost().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['rename'] != null) {
            queryParameters['rename'] = requestParameters['rename'];
        }

        if (requestParameters['overwrite'] != null) {
            queryParameters['overwrite'] = requestParameters['overwrite'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/restore`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Clear `deleted_at` + `purge_at` on a soft-deleted artifact. Available only while the artifact is in trash (i.e. before the GC cleanup cron purges it). Returns 404 if the artifact is live or already hard-deleted; 409 PATH_CONFLICT if its path is now occupied by another live artifact. The 409 payload includes a `restore_options` block with `rename_to` and `force_overwrite` URLs the caller can follow to resolve the conflict — see deletion-design.md §5.4.
     * Restore a soft-deleted artifact
     */
    async restoreArtifactV0ArtifactsArtIdRestorePostRaw(requestParameters: RestoreArtifactV0ArtifactsArtIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.restoreArtifactV0ArtifactsArtIdRestorePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Clear `deleted_at` + `purge_at` on a soft-deleted artifact. Available only while the artifact is in trash (i.e. before the GC cleanup cron purges it). Returns 404 if the artifact is live or already hard-deleted; 409 PATH_CONFLICT if its path is now occupied by another live artifact. The 409 payload includes a `restore_options` block with `rename_to` and `force_overwrite` URLs the caller can follow to resolve the conflict — see deletion-design.md §5.4.
     * Restore a soft-deleted artifact
     */
    async restoreArtifactV0ArtifactsArtIdRestorePost(requestParameters: RestoreArtifactV0ArtifactsArtIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.restoreArtifactV0ArtifactsArtIdRestorePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePost without sending the request
     */
    async restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequestOpts(requestParameters: RestoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePost().'
            );
        }

        if (requestParameters['versionNumber'] == null) {
            throw new runtime.RequiredError(
                'versionNumber',
                'Required parameter "versionNumber" was null or undefined when calling restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/artifacts/{art_id}/versions/{version_number}/restore`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));
        urlPath = urlPath.replace('{version_number}', encodeURIComponent(String(requestParameters['versionNumber'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Roll the artifact forward to the content of version `version_number` by creating a **new head version** with identical bytes. History is preserved — this never rewrites or deletes past versions. The prior version\'s content-addressed blob is reused, so no bytes are re-uploaded. A change summary of `Restored version N` is recorded on the new version; `X-AgentDrive-Actor` attributes it.  Restoring a version whose content already matches the current head (including the head itself) is a **no-op**: it returns the current artifact unchanged, with no new version created.  Honors `If-Match` on the current head (roll forward only if the head is unchanged → 412 PRECONDITION_FAILED).  Errors: `404 ARTIFACT_NOT_FOUND`, `404 VERSION_NOT_FOUND`, and `410 VERSION_PRUNED` when the version existed but its bytes were retained out of existence.
     * Restore a previous version as a new head version
     */
    async restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRaw(requestParameters: RestoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactOut>> {
        const requestOptions = await this.restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactOutFromJSON(jsonValue));
    }

    /**
     * Roll the artifact forward to the content of version `version_number` by creating a **new head version** with identical bytes. History is preserved — this never rewrites or deletes past versions. The prior version\'s content-addressed blob is reused, so no bytes are re-uploaded. A change summary of `Restored version N` is recorded on the new version; `X-AgentDrive-Actor` attributes it.  Restoring a version whose content already matches the current head (including the head itself) is a **no-op**: it returns the current artifact unchanged, with no new version created.  Honors `If-Match` on the current head (roll forward only if the head is unchanged → 412 PRECONDITION_FAILED).  Errors: `404 ARTIFACT_NOT_FOUND`, `404 VERSION_NOT_FOUND`, and `410 VERSION_PRUNED` when the version existed but its bytes were retained out of existence.
     * Restore a previous version as a new head version
     */
    async restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePost(requestParameters: RestoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactOut> {
        const response = await this.restoreArtifactVersionV0ArtifactsArtIdVersionsVersionNumberRestorePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for restoreDriveRouteV0DrivesDriveIdRestorePost without sending the request
     */
    async restoreDriveRouteV0DrivesDriveIdRestorePostRequestOpts(requestParameters: RestoreDriveRouteV0DrivesDriveIdRestorePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling restoreDriveRouteV0DrivesDriveIdRestorePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/restore`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Clear `deleted_at` + `purge_at` on a soft-deleted drive. Soft-deleted child artifacts get their retention window rebased to the drive-restore moment (see deletion-design.md §5.2). Available only while the drive is in trash. Returns 404 if the drive is live or already hard-deleted.  **Optimistic concurrency:** send `If-Match` with the trashed drive\'s composite ETag (`\"<drv_id>.0.<metageneration>\"`, e.g. from the delete response\'s `ETag` header) to make the restore conditional — a stale token returns 412 PRECONDITION_FAILED. A restore WITHOUT an `If-Match` precondition is last-writer-wins.
     * Restore a soft-deleted drive
     */
    async restoreDriveRouteV0DrivesDriveIdRestorePostRaw(requestParameters: RestoreDriveRouteV0DrivesDriveIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DriveRestoreOut>> {
        const requestOptions = await this.restoreDriveRouteV0DrivesDriveIdRestorePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DriveRestoreOutFromJSON(jsonValue));
    }

    /**
     * Clear `deleted_at` + `purge_at` on a soft-deleted drive. Soft-deleted child artifacts get their retention window rebased to the drive-restore moment (see deletion-design.md §5.2). Available only while the drive is in trash. Returns 404 if the drive is live or already hard-deleted.  **Optimistic concurrency:** send `If-Match` with the trashed drive\'s composite ETag (`\"<drv_id>.0.<metageneration>\"`, e.g. from the delete response\'s `ETag` header) to make the restore conditional — a stale token returns 412 PRECONDITION_FAILED. A restore WITHOUT an `If-Match` precondition is last-writer-wins.
     * Restore a soft-deleted drive
     */
    async restoreDriveRouteV0DrivesDriveIdRestorePost(requestParameters: RestoreDriveRouteV0DrivesDriveIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DriveRestoreOut> {
        const response = await this.restoreDriveRouteV0DrivesDriveIdRestorePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for restoreFolderByIdV0FoldersFldIdRestorePost without sending the request
     */
    async restoreFolderByIdV0FoldersFldIdRestorePostRequestOpts(requestParameters: RestoreFolderByIdV0FoldersFldIdRestorePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling restoreFolderByIdV0FoldersFldIdRestorePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['if-match'] = String(requestParameters['ifMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/folders/{fld_id}/restore`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Mirrors `POST /v0/artifacts/{art_id}/restore` for folders: clear `deleted_at` + `purge_at` on a soft-deleted folder AND exactly the descendants soft-deleted in the same cascade (descendants trashed separately keep their trash state; restore those individually — the per-artifact restore remains for cherry-picking). Available only while the folder is in trash; returns 404 if it is live or already hard-purged.  Returns 409 `PATH_CONFLICT` when a live folder/artifact now occupies a path this restore would reinstate (`colliding_path` + `kind` identify it). Unlike artifact restore there are NO `rename`/`overwrite` escape hatches — the whole cascade aborts; free the colliding path (or cherry-pick artifacts) and retry.  `If-Match` (the trashed folder\'s composite ETag) makes the restore conditional → 412 PRECONDITION_FAILED on a stale token; omitted, the restore is last-writer-wins.
     * Restore a soft-deleted folder (cascade)
     */
    async restoreFolderByIdV0FoldersFldIdRestorePostRaw(requestParameters: RestoreFolderByIdV0FoldersFldIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FolderRestoreOut>> {
        const requestOptions = await this.restoreFolderByIdV0FoldersFldIdRestorePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FolderRestoreOutFromJSON(jsonValue));
    }

    /**
     * Mirrors `POST /v0/artifacts/{art_id}/restore` for folders: clear `deleted_at` + `purge_at` on a soft-deleted folder AND exactly the descendants soft-deleted in the same cascade (descendants trashed separately keep their trash state; restore those individually — the per-artifact restore remains for cherry-picking). Available only while the folder is in trash; returns 404 if it is live or already hard-purged.  Returns 409 `PATH_CONFLICT` when a live folder/artifact now occupies a path this restore would reinstate (`colliding_path` + `kind` identify it). Unlike artifact restore there are NO `rename`/`overwrite` escape hatches — the whole cascade aborts; free the colliding path (or cherry-pick artifacts) and retry.  `If-Match` (the trashed folder\'s composite ETag) makes the restore conditional → 412 PRECONDITION_FAILED on a stale token; omitted, the restore is last-writer-wins.
     * Restore a soft-deleted folder (cascade)
     */
    async restoreFolderByIdV0FoldersFldIdRestorePost(requestParameters: RestoreFolderByIdV0FoldersFldIdRestorePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FolderRestoreOut> {
        const response = await this.restoreFolderByIdV0FoldersFldIdRestorePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for rotateShareRouteV0SharesShrIdRotatePost without sending the request
     */
    async rotateShareRouteV0SharesShrIdRotatePostRequestOpts(requestParameters: RotateShareRouteV0SharesShrIdRotatePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['shrId'] == null) {
            throw new runtime.RequiredError(
                'shrId',
                'Required parameter "shrId" was null or undefined when calling rotateShareRouteV0SharesShrIdRotatePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xAgentdriveActor'] != null) {
            headerParameters['x-agentdrive-actor'] = String(requestParameters['xAgentdriveActor']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/shares/{shr_id}/rotate`;
        urlPath = urlPath.replace('{shr_id}', encodeURIComponent(String(requestParameters['shrId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke + reissue a share link\'s key (requires can_share)
     */
    async rotateShareRouteV0SharesShrIdRotatePostRaw(requestParameters: RotateShareRouteV0SharesShrIdRotatePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareMintOut>> {
        const requestOptions = await this.rotateShareRouteV0SharesShrIdRotatePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareMintOutFromJSON(jsonValue));
    }

    /**
     * Revoke + reissue a share link\'s key (requires can_share)
     */
    async rotateShareRouteV0SharesShrIdRotatePost(requestParameters: RotateShareRouteV0SharesShrIdRotatePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareMintOut> {
        const response = await this.rotateShareRouteV0SharesShrIdRotatePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchV0SearchGet without sending the request
     */
    async searchV0SearchGetRequestOpts(requestParameters: SearchV0SearchGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['q'] == null) {
            throw new runtime.RequiredError(
                'q',
                'Required parameter "q" was null or undefined when calling searchV0SearchGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['label'] != null) {
            queryParameters['label'] = requestParameters['label'];
        }

        if (requestParameters['fileType'] != null) {
            queryParameters['file_type'] = requestParameters['fileType'];
        }

        if (requestParameters['prefix'] != null) {
            queryParameters['prefix'] = requestParameters['prefix'];
        }

        if (requestParameters['updatedAfter'] != null) {
            queryParameters['updated_after'] = (requestParameters['updatedAfter'] as any).toISOString();
        }

        if (requestParameters['updatedBefore'] != null) {
            queryParameters['updated_before'] = (requestParameters['updatedBefore'] as any).toISOString();
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/search`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Lexical (not semantic) full-text search powered by Postgres `websearch_to_tsquery`. Results are ranked by `ts_rank` over a weighted tsvector (path > content > metadata > labels).  **Supported query syntax:** - Words: `kangaroo` (English stemming) - Phrases: `\"exact phrase\"` - Negation: `kangaroo -secret` - AND (implicit): `kangaroo secret` - OR: `kangaroo OR koala` - Paths & filenames: `reports/q3-summary.md` or `q3-summary.md` match by their path words (`/ . _ -` are word boundaries)  **Not supported (v0):** - Semantic / embedding similarity - PDF and image content (only the path + metadata are searchable) - Non-English stemming - Fuzzy matching, regex - Boolean operator parentheses  **Filters:** `label` (repeatable, AND), `file_type` (enum), `prefix` (path prefix), `updated_after` / `updated_before` (RFC 3339 timestamps, inclusive bounds on `updated_at`).
     * Full-text search over artifacts in the drive
     */
    async searchV0SearchGetRaw(requestParameters: SearchV0SearchGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchPage>> {
        const requestOptions = await this.searchV0SearchGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchPageFromJSON(jsonValue));
    }

    /**
     * Lexical (not semantic) full-text search powered by Postgres `websearch_to_tsquery`. Results are ranked by `ts_rank` over a weighted tsvector (path > content > metadata > labels).  **Supported query syntax:** - Words: `kangaroo` (English stemming) - Phrases: `\"exact phrase\"` - Negation: `kangaroo -secret` - AND (implicit): `kangaroo secret` - OR: `kangaroo OR koala` - Paths & filenames: `reports/q3-summary.md` or `q3-summary.md` match by their path words (`/ . _ -` are word boundaries)  **Not supported (v0):** - Semantic / embedding similarity - PDF and image content (only the path + metadata are searchable) - Non-English stemming - Fuzzy matching, regex - Boolean operator parentheses  **Filters:** `label` (repeatable, AND), `file_type` (enum), `prefix` (path prefix), `updated_after` / `updated_before` (RFC 3339 timestamps, inclusive bounds on `updated_at`).
     * Full-text search over artifacts in the drive
     */
    async searchV0SearchGet(requestParameters: SearchV0SearchGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchPage> {
        const response = await this.searchV0SearchGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for viewArtifactHeadAArtIdHeadGet without sending the request
     */
    async viewArtifactHeadAArtIdHeadGetRequestOpts(requestParameters: ViewArtifactHeadAArtIdHeadGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling viewArtifactHeadAArtIdHeadGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/a/{art_id}/head`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return `{\"version\": <head version_number>}` for a readable artifact.  Auth mirrors the permalink/viewer: the owner, or an `anyone:viewer` grant (a published artifact), reads. Two deliberate differences from the HTML viewer:    * Never redirect to login. A poll is a background `fetch`, not a     navigation — an HTML login page would be a useless body and a     same-origin redirect the client can\'t act on. Anonymous callers     on a private/absent artifact get a flat 404.   * \"Doesn\'t exist\" and \"exists but not readable\" collapse to the     same 404, so an anonymous poller can\'t use this as an existence     oracle (matches the permalink/viewer leak guard).
     * View Artifact Head
     */
    async viewArtifactHeadAArtIdHeadGetRaw(requestParameters: ViewArtifactHeadAArtIdHeadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactHeadOut>> {
        const requestOptions = await this.viewArtifactHeadAArtIdHeadGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactHeadOutFromJSON(jsonValue));
    }

    /**
     * Return `{\"version\": <head version_number>}` for a readable artifact.  Auth mirrors the permalink/viewer: the owner, or an `anyone:viewer` grant (a published artifact), reads. Two deliberate differences from the HTML viewer:    * Never redirect to login. A poll is a background `fetch`, not a     navigation — an HTML login page would be a useless body and a     same-origin redirect the client can\'t act on. Anonymous callers     on a private/absent artifact get a flat 404.   * \"Doesn\'t exist\" and \"exists but not readable\" collapse to the     same 404, so an anonymous poller can\'t use this as an existence     oracle (matches the permalink/viewer leak guard).
     * View Artifact Head
     */
    async viewArtifactHeadAArtIdHeadGet(requestParameters: ViewArtifactHeadAArtIdHeadGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactHeadOut> {
        const response = await this.viewArtifactHeadAArtIdHeadGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for viewArtifactVersionVArtIdVersionGet without sending the request
     */
    async viewArtifactVersionVArtIdVersionGetRequestOpts(requestParameters: ViewArtifactVersionVArtIdVersionGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling viewArtifactVersionVArtIdVersionGet().'
            );
        }

        if (requestParameters['version'] == null) {
            throw new runtime.RequiredError(
                'version',
                'Required parameter "version" was null or undefined when calling viewArtifactVersionVArtIdVersionGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['raw'] != null) {
            queryParameters['raw'] = requestParameters['raw'];
        }

        if (requestParameters['download'] != null) {
            queryParameters['download'] = requestParameters['download'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v/{art_id}/{version}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));
        urlPath = urlPath.replace('{version}', encodeURIComponent(String(requestParameters['version'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Render version `version` of an artifact, read-only.  Version history is owner-only. The drive-blind `can_read` gate still provides the same sign-in-or-404 masking as `/a/{art_id}`, but readable non-owners cannot browse snapshots. A pruned or never-existed version renders a friendly unavailable state, never a 500. `?raw=1` / `?download=1` stream the version\'s bytes (powering the bar\'s Raw / Download buttons) with the same sandbox+nosniff headers as the head raw path.
     * View Artifact Version
     */
    async viewArtifactVersionVArtIdVersionGetRaw(requestParameters: ViewArtifactVersionVArtIdVersionGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.viewArtifactVersionVArtIdVersionGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Render version `version` of an artifact, read-only.  Version history is owner-only. The drive-blind `can_read` gate still provides the same sign-in-or-404 masking as `/a/{art_id}`, but readable non-owners cannot browse snapshots. A pruned or never-existed version renders a friendly unavailable state, never a 500. `?raw=1` / `?download=1` stream the version\'s bytes (powering the bar\'s Raw / Download buttons) with the same sandbox+nosniff headers as the head raw path.
     * View Artifact Version
     */
    async viewArtifactVersionVArtIdVersionGet(requestParameters: ViewArtifactVersionVArtIdVersionGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.viewArtifactVersionVArtIdVersionGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for viewFileDriveIdPathGet without sending the request
     */
    async viewFileDriveIdPathGetRequestOpts(requestParameters: ViewFileDriveIdPathGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling viewFileDriveIdPathGet().'
            );
        }

        if (requestParameters['path'] == null) {
            throw new runtime.RequiredError(
                'path',
                'Required parameter "path" was null or undefined when calling viewFileDriveIdPathGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['raw'] != null) {
            queryParameters['raw'] = requestParameters['raw'];
        }

        if (requestParameters['download'] != null) {
            queryParameters['download'] = requestParameters['download'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/{drive_id}/{path}`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));
        urlPath = urlPath.replace('{path}', encodeURIComponent(String(requestParameters['path'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * View File
     */
    async viewFileDriveIdPathGetRaw(requestParameters: ViewFileDriveIdPathGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.viewFileDriveIdPathGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * View File
     */
    async viewFileDriveIdPathGet(requestParameters: ViewFileDriveIdPathGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.viewFileDriveIdPathGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for viewPermalinkArtifactAArtIdGet without sending the request
     */
    async viewPermalinkArtifactAArtIdGetRequestOpts(requestParameters: ViewPermalinkArtifactAArtIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['artId'] == null) {
            throw new runtime.RequiredError(
                'artId',
                'Required parameter "artId" was null or undefined when calling viewPermalinkArtifactAArtIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/a/{art_id}`;
        urlPath = urlPath.replace('{art_id}', encodeURIComponent(String(requestParameters['artId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Resolve a stable artifact ID to its path-URL and 302 there.  Auth model matches the path URL: public artifacts redirect for anyone; private artifacts redirect only for the owner. Non-owners on private artifacts get 404 — same response as \"doesn\'t exist\", so the ID\'s existence isn\'t leaked. The forwarded query-param allowlist is `raw`, `download` (see _PERMALINK_FORWARDED_PARAMS).
     * View Permalink Artifact
     */
    async viewPermalinkArtifactAArtIdGetRaw(requestParameters: ViewPermalinkArtifactAArtIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.viewPermalinkArtifactAArtIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Resolve a stable artifact ID to its path-URL and 302 there.  Auth model matches the path URL: public artifacts redirect for anyone; private artifacts redirect only for the owner. Non-owners on private artifacts get 404 — same response as \"doesn\'t exist\", so the ID\'s existence isn\'t leaked. The forwarded query-param allowlist is `raw`, `download` (see _PERMALINK_FORWARDED_PARAMS).
     * View Permalink Artifact
     */
    async viewPermalinkArtifactAArtIdGet(requestParameters: ViewPermalinkArtifactAArtIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.viewPermalinkArtifactAArtIdGetRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for viewPermalinkFolderFFldIdGet without sending the request
     */
    async viewPermalinkFolderFFldIdGetRequestOpts(requestParameters: ViewPermalinkFolderFFldIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['fldId'] == null) {
            throw new runtime.RequiredError(
                'fldId',
                'Required parameter "fldId" was null or undefined when calling viewPermalinkFolderFFldIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/f/{fld_id}`;
        urlPath = urlPath.replace('{fld_id}', encodeURIComponent(String(requestParameters['fldId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Resolve a stable folder ID to its current path-URL and 302.  Auth model mirrors the artifact permalink: public folder = anon OK; private folder = owner only, otherwise 404 (no existence leak). \"Public\" is an `anyone:viewer` grant on the `fld_*` id resolved through `can_read` (§4.4); folders carry no visibility flag of their own.
     * View Permalink Folder
     */
    async viewPermalinkFolderFFldIdGetRaw(requestParameters: ViewPermalinkFolderFFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.viewPermalinkFolderFFldIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Resolve a stable folder ID to its current path-URL and 302.  Auth model mirrors the artifact permalink: public folder = anon OK; private folder = owner only, otherwise 404 (no existence leak). \"Public\" is an `anyone:viewer` grant on the `fld_*` id resolved through `can_read` (§4.4); folders carry no visibility flag of their own.
     * View Permalink Folder
     */
    async viewPermalinkFolderFFldIdGet(requestParameters: ViewPermalinkFolderFFldIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.viewPermalinkFolderFFldIdGetRaw(requestParameters, initOverrides);
    }

}

/**
 * @export
 */
export const FindV0FindGetModeEnum = {
    Hybrid: 'hybrid',
    Lexical: 'lexical',
    Semantic: 'semantic'
} as const;
export type FindV0FindGetModeEnum = typeof FindV0FindGetModeEnum[keyof typeof FindV0FindGetModeEnum];
