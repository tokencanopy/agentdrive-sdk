/* tslint:disable */
/* eslint-disable */
/**
 * AgentDrive
 * AgentDrive is an agent-focused artifact store: upload by path, share by rendered URL, address by stable permalink. The REST surface is documented here; the rendered viewer + agent claim flow live under `agentdrive.run`.
 *
 * The version of the OpenAPI document: <PINNED>
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type UserTokenList,
    UserTokenListFromJSON,
    UserTokenListToJSON,
} from '../models/UserTokenList';
import {
    type UserTokenOut,
    UserTokenOutFromJSON,
    UserTokenOutToJSON,
} from '../models/UserTokenOut';
import {
    type ValidationErrorResponse,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/ValidationErrorResponse';

export interface ListTokensV0TokensGetRequest {
    cursor?: string | null;
    limit?: number | null;
}

export interface RevokeTokenV0TokensTokenIdRevokePostRequest {
    tokenId: string;
}

/**
 *
 */
export class TokensApi extends runtime.BaseAPI {

    /**
     * Creates request options for listTokensV0TokensGet without sending the request
     */
    async listTokensV0TokensGetRequestOpts(requestParameters: ListTokensV0TokensGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/tokens`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the `ad_user_` tokens belonging to the authenticated user. Metadata only — the raw token is shown once at mint (web only) and is never returned here. Includes recently-revoked tokens (with `revoked_at` set) so the caller can audit them; newest first.  **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected.
     * List your user-identity tokens
     */
    async listTokensV0TokensGetRaw(requestParameters: ListTokensV0TokensGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserTokenList>> {
        const requestOptions = await this.listTokensV0TokensGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserTokenListFromJSON(jsonValue));
    }

    /**
     * List the `ad_user_` tokens belonging to the authenticated user. Metadata only — the raw token is shown once at mint (web only) and is never returned here. Includes recently-revoked tokens (with `revoked_at` set) so the caller can audit them; newest first.  **Cursor pagination:** when more results exist, the response carries `next_cursor`. Pass it back as `?cursor=<token>` to fetch the next page; `null` means the listing is complete. `limit` is clamped to [1, 100] (default 50), never rejected.
     * List your user-identity tokens
     */
    async listTokensV0TokensGet(requestParameters: ListTokensV0TokensGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserTokenList> {
        const response = await this.listTokensV0TokensGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for revokeTokenV0TokensTokenIdRevokePost without sending the request
     */
    async revokeTokenV0TokensTokenIdRevokePostRequestOpts(requestParameters: RevokeTokenV0TokensTokenIdRevokePostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['tokenId'] == null) {
            throw new runtime.RequiredError(
                'tokenId',
                'Required parameter "tokenId" was null or undefined when calling revokeTokenV0TokensTokenIdRevokePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/tokens/{token_id}/revoke`;
        urlPath = urlPath.replace('{token_id}', encodeURIComponent(String(requestParameters['tokenId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke a single `ad_user_` token by id. Scoped to the authenticated user: a token id that isn\'t yours returns 404 (no-leak). Idempotent — revoking an already-revoked token also returns 404 (it is no longer a live token of yours to revoke). On success the revoked token\'s metadata is returned with `revoked_at` set.
     * Revoke one of your user-identity tokens
     */
    async revokeTokenV0TokensTokenIdRevokePostRaw(requestParameters: RevokeTokenV0TokensTokenIdRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserTokenOut>> {
        const requestOptions = await this.revokeTokenV0TokensTokenIdRevokePostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserTokenOutFromJSON(jsonValue));
    }

    /**
     * Revoke a single `ad_user_` token by id. Scoped to the authenticated user: a token id that isn\'t yours returns 404 (no-leak). Idempotent — revoking an already-revoked token also returns 404 (it is no longer a live token of yours to revoke). On success the revoked token\'s metadata is returned with `revoked_at` set.
     * Revoke one of your user-identity tokens
     */
    async revokeTokenV0TokensTokenIdRevokePost(requestParameters: RevokeTokenV0TokensTokenIdRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserTokenOut> {
        const response = await this.revokeTokenV0TokensTokenIdRevokePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
