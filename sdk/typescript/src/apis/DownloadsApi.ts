/* tslint:disable */
/* eslint-disable */
/**
 * AgentDrive
 * AgentDrive is an agent-focused artifact store: drive-scoped folders, artifacts, and immutable versions, with local grants, possession-based share links, drive-scoped search, and a cursor-resumable change feed. Bearer-authenticated with Hub-issued product tokens (see /.well-known/oauth-protected-resource); every mutation takes an Idempotency-Key, and existing-state mutations take If-Match.
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type DownloadCapabilitiesCreateRequest,
    DownloadCapabilitiesCreateRequestFromJSON,
    DownloadCapabilitiesCreateRequestToJSON,
} from '../models/DownloadCapabilitiesCreateRequest';
import {
    type DownloadCapabilityOut,
    DownloadCapabilityOutFromJSON,
    DownloadCapabilityOutToJSON,
} from '../models/DownloadCapabilityOut';
import {
    type V0ErrorEnvelope,
    V0ErrorEnvelopeFromJSON,
    V0ErrorEnvelopeToJSON,
} from '../models/V0ErrorEnvelope';

export interface DownloadCapabilitiesCreateOperationRequest {
    /**
     * 
     */
    driveId: string;
    /**
     * 
     */
    downloadCapabilitiesCreateRequest: DownloadCapabilitiesCreateRequest;
    /**
     * Deprecated: redundant with the operation's `bearerAuth` security requirement, which is how a generated client should learn to authenticate. Scheduled for removal.
     * @deprecated
     */
    authorization?: string | null;
}

/**
 * 
 */
export class DownloadsApi extends runtime.BaseAPI {

    /**
     * Creates request options for downloadCapabilitiesCreate without sending the request
     */
    async downloadCapabilitiesCreateRequestOpts(requestParameters: DownloadCapabilitiesCreateOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling downloadCapabilitiesCreate().'
            );
        }

        if (requestParameters['downloadCapabilitiesCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'downloadCapabilitiesCreateRequest',
                'Required parameter "downloadCapabilitiesCreateRequest" was null or undefined when calling downloadCapabilitiesCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/download-capabilities`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DownloadCapabilitiesCreateRequestToJSON(requestParameters['downloadCapabilitiesCreateRequest']),
        };
    }

    /**
     * Mint one fresh, generation-pinned signed GET target for the current artifact head or one owned version. 200 only; every call reauthorizes and re-mints.  Strict JSON body (charset utf-8): unknown/duplicate fields, unknown discriminators, and malformed ids are 400 INVALID_REQUEST. Idempotency-Key is FORBIDDEN on this operation (manifest idempotency_class: forbidden): a supplied key is rejected with 400 INVALID_REQUEST and no idempotency record is created — every request reauthorizes and mints a fresh signed target. The signed URL is a bearer capability after disclosure: it is bucket/object-, generation-, method-, semantic-query-, and expiry-bound only (no one-time-use or audience enforcement).
     * Create Download Capability
     */
    async downloadCapabilitiesCreateRaw(requestParameters: DownloadCapabilitiesCreateOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DownloadCapabilityOut>> {
        const requestOptions = await this.downloadCapabilitiesCreateRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DownloadCapabilityOutFromJSON(jsonValue));
    }

    /**
     * Mint one fresh, generation-pinned signed GET target for the current artifact head or one owned version. 200 only; every call reauthorizes and re-mints.  Strict JSON body (charset utf-8): unknown/duplicate fields, unknown discriminators, and malformed ids are 400 INVALID_REQUEST. Idempotency-Key is FORBIDDEN on this operation (manifest idempotency_class: forbidden): a supplied key is rejected with 400 INVALID_REQUEST and no idempotency record is created — every request reauthorizes and mints a fresh signed target. The signed URL is a bearer capability after disclosure: it is bucket/object-, generation-, method-, semantic-query-, and expiry-bound only (no one-time-use or audience enforcement).
     * Create Download Capability
     */
    async downloadCapabilitiesCreate(requestParameters: DownloadCapabilitiesCreateOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DownloadCapabilityOut> {
        const response = await this.downloadCapabilitiesCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
