/* tslint:disable */
/* eslint-disable */
/**
 * AgentDrive
 * AgentDrive is an agent-focused artifact store: drive-scoped folders, artifacts, and immutable versions, with local grants, possession-based share links, drive-scoped search, and a cursor-resumable change feed. Bearer-authenticated with Hub-issued product tokens (see /.well-known/oauth-protected-resource); every mutation takes an Idempotency-Key, and existing-state mutations take If-Match.
 *
 * The version of the OpenAPI document: <PINNED>
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type DrivesCreate400Response,
    DrivesCreate400ResponseFromJSON,
    DrivesCreate400ResponseToJSON,
} from '../models/DrivesCreate400Response';
import {
    type ShareCreateIn,
    ShareCreateInFromJSON,
    ShareCreateInToJSON,
} from '../models/ShareCreateIn';
import {
    type ShareCreateOut,
    ShareCreateOutFromJSON,
    ShareCreateOutToJSON,
} from '../models/ShareCreateOut';
import {
    type ShareListOut,
    ShareListOutFromJSON,
    ShareListOutToJSON,
} from '../models/ShareListOut';
import {
    type ShareOut,
    ShareOutFromJSON,
    ShareOutToJSON,
} from '../models/ShareOut';
import {
    type ValidationErrorResponse,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/ValidationErrorResponse';

export interface SharesCreateRequest {
    driveId: string;
    idempotencyKey: string | null;
    shareCreateIn: ShareCreateIn;
    authorization?: string | null;
}

export interface SharesListRequest {
    driveId: string;
    lifecycle?: string;
    limit?: number | null;
    cursor?: string | null;
    resourceType?: string | null;
    resourceId?: string | null;
    authorization?: string | null;
}

export interface SharesReadRequest {
    driveId: string;
    shareId: string;
    ifNoneMatch?: string | null;
    authorization?: string | null;
}

export interface SharesRevokeRequest {
    driveId: string;
    shareId: string;
    idempotencyKey: string | null;
    ifMatch: string | null;
    authorization?: string | null;
}

export interface SharesRotateRequest {
    driveId: string;
    shareId: string;
    idempotencyKey: string | null;
    ifMatch: string | null;
    authorization?: string | null;
}

/**
 *
 */
export class SharesApi extends runtime.BaseAPI {

    /**
     * Creates request options for sharesCreate without sending the request
     */
    async sharesCreateRequestOpts(requestParameters: SharesCreateRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling sharesCreate().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling sharesCreate().'
            );
        }

        if (requestParameters['shareCreateIn'] == null) {
            throw new runtime.RequiredError(
                'shareCreateIn',
                'Required parameter "shareCreateIn" was null or undefined when calling sharesCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/shares`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ShareCreateInToJSON(requestParameters['shareCreateIn']),
        };
    }

    /**
     * Mint a read-only bearer link. The response carries the plaintext secret — the only response that does.
     * Create Share
     */
    async sharesCreateRaw(requestParameters: SharesCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareCreateOut>> {
        const requestOptions = await this.sharesCreateRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareCreateOutFromJSON(jsonValue));
    }

    /**
     * Mint a read-only bearer link. The response carries the plaintext secret — the only response that does.
     * Create Share
     */
    async sharesCreate(requestParameters: SharesCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareCreateOut> {
        const response = await this.sharesCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for sharesList without sending the request
     */
    async sharesListRequestOpts(requestParameters: SharesListRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling sharesList().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['lifecycle'] != null) {
            queryParameters['lifecycle'] = requestParameters['lifecycle'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['resourceType'] != null) {
            queryParameters['resource_type'] = requestParameters['resourceType'];
        }

        if (requestParameters['resourceId'] != null) {
            queryParameters['resource_id'] = requestParameters['resourceId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/shares`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the drive\'s shares (no secrets), keyset paginated.  ``resource_id`` narrows the page to one resource\'s links and REQUIRES ``resource_type`` alongside it — a bare resource id is ambiguous across ``artifact`` / ``artifact_version`` / ``folder``, and inferring the kind from the id prefix would tie the filter\'s meaning to an id format the contract does not promise to keep. ``resource_type`` alone is a valid filter. Listing shares already requires drive ``manager``, so these filters only narrow a page the caller could already read in full.
     * List Shares
     */
    async sharesListRaw(requestParameters: SharesListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareListOut>> {
        const requestOptions = await this.sharesListRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareListOutFromJSON(jsonValue));
    }

    /**
     * List the drive\'s shares (no secrets), keyset paginated.  ``resource_id`` narrows the page to one resource\'s links and REQUIRES ``resource_type`` alongside it — a bare resource id is ambiguous across ``artifact`` / ``artifact_version`` / ``folder``, and inferring the kind from the id prefix would tie the filter\'s meaning to an id format the contract does not promise to keep. ``resource_type`` alone is a valid filter. Listing shares already requires drive ``manager``, so these filters only narrow a page the caller could already read in full.
     * List Shares
     */
    async sharesList(requestParameters: SharesListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareListOut> {
        const response = await this.sharesListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for sharesRead without sending the request
     */
    async sharesReadRequestOpts(requestParameters: SharesReadRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling sharesRead().'
            );
        }

        if (requestParameters['shareId'] == null) {
            throw new runtime.RequiredError(
                'shareId',
                'Required parameter "shareId" was null or undefined when calling sharesRead().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['If-None-Match'] = String(requestParameters['ifNoneMatch']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/shares/{share_id}`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));
        urlPath = urlPath.replace('{share_id}', encodeURIComponent(String(requestParameters['shareId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Read one share\'s management representation (no secret).
     * Read Share
     */
    async sharesReadRaw(requestParameters: SharesReadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareOut>> {
        const requestOptions = await this.sharesReadRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareOutFromJSON(jsonValue));
    }

    /**
     * Read one share\'s management representation (no secret).
     * Read Share
     */
    async sharesRead(requestParameters: SharesReadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareOut> {
        const response = await this.sharesReadRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for sharesRevoke without sending the request
     */
    async sharesRevokeRequestOpts(requestParameters: SharesRevokeRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling sharesRevoke().'
            );
        }

        if (requestParameters['shareId'] == null) {
            throw new runtime.RequiredError(
                'shareId',
                'Required parameter "shareId" was null or undefined when calling sharesRevoke().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling sharesRevoke().'
            );
        }

        if (requestParameters['ifMatch'] == null) {
            throw new runtime.RequiredError(
                'ifMatch',
                'Required parameter "ifMatch" was null or undefined when calling sharesRevoke().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['If-Match'] = String(requestParameters['ifMatch']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/shares/{share_id}`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));
        urlPath = urlPath.replace('{share_id}', encodeURIComponent(String(requestParameters['shareId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke a share (soft, sets revoked_at) under If-Match.
     * Revoke Share
     */
    async sharesRevokeRaw(requestParameters: SharesRevokeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareOut>> {
        const requestOptions = await this.sharesRevokeRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareOutFromJSON(jsonValue));
    }

    /**
     * Revoke a share (soft, sets revoked_at) under If-Match.
     * Revoke Share
     */
    async sharesRevoke(requestParameters: SharesRevokeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareOut> {
        const response = await this.sharesRevokeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for sharesRotate without sending the request
     */
    async sharesRotateRequestOpts(requestParameters: SharesRotateRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['driveId'] == null) {
            throw new runtime.RequiredError(
                'driveId',
                'Required parameter "driveId" was null or undefined when calling sharesRotate().'
            );
        }

        if (requestParameters['shareId'] == null) {
            throw new runtime.RequiredError(
                'shareId',
                'Required parameter "shareId" was null or undefined when calling sharesRotate().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling sharesRotate().'
            );
        }

        if (requestParameters['ifMatch'] == null) {
            throw new runtime.RequiredError(
                'ifMatch',
                'Required parameter "ifMatch" was null or undefined when calling sharesRotate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['ifMatch'] != null) {
            headerParameters['If-Match'] = String(requestParameters['ifMatch']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/drives/{drive_id}/shares/{share_id}/rotate`;
        urlPath = urlPath.replace('{drive_id}', encodeURIComponent(String(requestParameters['driveId'])));
        urlPath = urlPath.replace('{share_id}', encodeURIComponent(String(requestParameters['shareId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Rotate the secret in place (same id, no grace window). The response carries the new plaintext secret.
     * Rotate Share
     */
    async sharesRotateRaw(requestParameters: SharesRotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShareCreateOut>> {
        const requestOptions = await this.sharesRotateRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShareCreateOutFromJSON(jsonValue));
    }

    /**
     * Rotate the secret in place (same id, no grace window). The response carries the new plaintext secret.
     * Rotate Share
     */
    async sharesRotate(requestParameters: SharesRotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShareCreateOut> {
        const response = await this.sharesRotateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
