name: SDK contract

on:
  workflow_dispatch: {}
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: sdk-contract-${{ github.ref }}
  cancel-in-progress: true

jobs:
  acceptance:
    uses: ./.github/workflows/_sdk-acceptance.yml
