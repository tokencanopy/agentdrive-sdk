name: Publish SDKs

# Publishes the generated SDKs:
#   - Python  -> PyPI  (agentdrive-sdk)        via OIDC trusted publishing (no token)
#   - TS      -> npm   (@tokencanopy/agentdrive-sdk) via npm trusted publishing
#   - Go      -> git tag go/vX.Y.Z              (consumed with `go get .../go@vX.Y.Z`)
#
# Publishes the version currently baked into the generated package metadata.
# Bump by re-running "Generate SDKs" with SDK_VERSION set, then release.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version being published (e.g. 0.0.1), used for the Go tag."
        required: true
        default: "0.0.1"

jobs:
  pypi:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # OIDC trusted publishing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build sdist + wheel
        working-directory: sdk/python
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: sdk/python/dist
          skip-existing: true

  npm:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # OIDC trusted publishing (no NPM token)
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
      # Trusted publishing needs npm >= 11.5.1, so the npm that ships with
      # Node may be too old and gets upgraded here.
      #
      # Both halves of this are deliberate. Node is 24 rather than 20 because
      # npm@latest reached 12, which requires
      # `^22.22.2 || ^24.15.0 || >=26.0.0` — on Node 20 the upgrade below
      # aborted with `notsup` and the 0.0.2 release failed at this step. And
      # the upgrade is bounded to ^11 rather than `latest` because `latest`
      # is what moved: an unbounded install re-breaks this job the day npm 13
      # raises its floor past whatever Node is pinned above. ^11.5.1 states
      # the requirement the comment already describes.
      - name: Upgrade npm for OIDC trusted publishing
        run: npm install -g "npm@^11.5.1"
      - name: Build & publish (OIDC, no token)
        working-directory: sdk/typescript
        run: |
          npm install
          npm run build
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "@tokencanopy/agentdrive-sdk@${VERSION}" version >/dev/null 2>&1; then
            echo "npm: @tokencanopy/agentdrive-sdk@${VERSION} already published — skipping."
            exit 0
          fi
          # `npm view` reads a replica that lags the authoritative write path,
          # so a freshly/concurrently published version can still 403 here.
          # Treat "cannot publish over previously published" as success.
          set +e
          OUT="$(npm publish --access public 2>&1)"; CODE=$?
          set -e
          printf '%s\n' "$OUT"
          if [ "$CODE" -ne 0 ] && ! printf '%s' "$OUT" | grep -q "cannot publish over the previously published versions"; then
            exit "$CODE"
          fi

  go-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Create Go submodule version tag
        run: |
          set -euo pipefail
          RAW="${{ github.event.inputs.version || github.event.release.tag_name }}"
          VERSION="${RAW#v}"
          TAG="sdk/go/v${VERSION}"
          git config user.name  "agentdrive-bot"
          git config user.email "support@agentdrive.run"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists."
          else
            git tag "$TAG"
            git push origin "$TAG"
          fi
